__pycache__/
*.py[cod]
.venv/
venv/

# Uretim ciktisi (8.5M hane) - repoya girmez, calistirinca yeniden uretilir.
# data/tuik/ ise BILEREK commit'li: girdi kaynagi, uretimi tekrarlanabilir kiliyor.
data/generated/

# EPİAŞ ham yanıt cache'i (src/epias_cache.py). Uretim ciktisi gibi: repoya girmez,
# gerektiginde EPIAS'tan yeniden cekilir.
data/epias/

# Adim 2b: Open-Meteo sicaklik cache'i, .env gibi yeniden uretilebilir/yerel veri.
data/weather/

.env
.envrc.local

# Claude Code oturum artifaktlari. Ozellikle .claude/worktrees/ altinda tam bir git
# worktree durabiliyor; `git add -A` ile yanlislikla commit'e girmesin.
.claude/
