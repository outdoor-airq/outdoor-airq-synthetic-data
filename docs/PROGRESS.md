# Aşama Onay Geçmişi

Format: `- [tarih] stageN: yapılan iş özeti — onaylayan: (isim)`

Claude Code bu dosyaya sadece kullanıcı onayı sonrası satır ekler, sonra commit atar.

<!-- örnek:
- [2026-07-06] stage1: modüler mqtt subscriber + sqlalchemy modelleri, .env geçişi — onaylayan: kullanıcı
-->

- [2026-07-06] stage1: mqtt/ modüler subscriber yapısına geçirildi (db.py, models.py, handlers.py, consumer.py), publisher.py ve pg_setup.py .env'den okuyacak şekilde güncellendi — onaylayan: kullanıcı
- [2026-07-06] stage1: dedup kontrolü (aynı measured_at'li mesaj atlanır) ve filtered_readings z-score katmanı (baseline DB'de tek sorguda hesaplanıyor, is_valid/is_anomaly), measured_at datetime dönüşüm hatası düzeltildi, test edildi — onaylayan: kullanıcı
- [2026-07-06] stage2: db.py/models.py mqtt/subscriber'dan kök seviyeye shared/ paketine taşındı (mqtt ve backend arasında paylaşılıyor, kod tekrarı yok), backend/ FastAPI BFF ile 5 endpoint eklendi (GET /stations, /stations/{id}/latest, /stations/{id}/history?range=24h|7d|30d, /readings/latest, /readings/anomalies?limit=50), Pydantic response modelleri + FastAPI dependency injection ile DB session yönetimi, CORS geliştirme için tüm origin'lere açık, hem mqtt hem backend gerçek DB'ye karşı canlı test edildi — onaylayan: kullanıcı
- [2026-07-07] stage3a: frontend/ React (Vite) iskeleti + react-leaflet harita, /readings/latest verisiyle İstanbul istasyonları için AQI rozet marker'ları (DivIcon, ortada AQI sayısı, kategori rengi), marker'a tıklanınca basit detay paneli (AQI/kategori/baskın kirletici/ölçüm zamanı, henüz grafik yok), canlı test edildi — onaylayan: kullanıcı
- [2026-07-07] stage3b: HistoryPointOut şemasına pm25/pm10/o3/no2/so2/co/temperature/humidity/wind eklendi; detay drawer/mobil tam ekran akışı (Chart.js zaman serisi + kirletici radar + 7g günlük ort/min/maks kartları, 24s/7g/30g seçici), responsive kırılım 768px; kategori göstergesi SVG nokta (emoji yok); harita Türkiye geneli maxBounds + yumuşak sınırlama; panel aç/kapa (collapse) toggle; varsayılan panelde AQI'ye göre azalan istasyon sıralaması (satıra tıklama = marker seçimi), canlı test edildi — onaylayan: kullanıcı
- [2026-07-07] stage3b-iyileştirme: seçimde haritada animasyonlu flyTo (zoom 13); "Detayları Göster" drawer yerine tam sayfa detay ekranına dönüştürüldü (React Router yok, view state; masaüstü+mobil aynı StationDetailPage'i paylaşır, "Haritaya Dön" butonu); kirletici radarı IQAir tarzı 6 karta çevrildi (Türkçe adlar, µg/m³); baskın kirletici kategori renkli kutuya alındı (değer + µg/m³); WeatherCard eklendi (sıcaklık/nem/rüzgar m/s + veri kaynağı) hem detay sayfasında hem özet panelde; özet panelde "Detayları Göster" alta sabit footer, canlı test edildi — onaylayan: kullanıcı
- [2026-07-07] stage3c: leaflet.heat ile AQI enterpolasyon heatmap katmanı (kategori renkleriyle uyumlu gradient); sağ üstte bağımsız açılıp kapanan katman kontrol paneli (İstasyonlar / Isı haritası / Anomaliler); anomali katmanı is_anomaly=true istasyonları kırmızı kesikli halkayla vurgular; frontend/CLAUDE.md'ye "100+ sensöre ölçeklenirken dikkat edilecekler" notları (markercluster, /readings/latest sayfalama/bbox, heatmap yoğun veride iyileşir); mqtt/CLAUDE.md'ye baseline_std floor teknik borç notu, canlı test edildi — onaylayan: kullanıcı
- [2026-07-07] stage3d: yeni backend endpoint GET /stations/{id}/calendar (SQL percentile_cont ile günlük PM2.5/PM10 çeyrekleri + anomaly_count/evaluated_count; NULL is_anomaly paydadan dışlanır, gün İstanbul yereline göre); StationDetailPage'de custom SVG takvim heatmap (GitHub-tarzı, yıl-yıl, ay sınır çizgileri), PM2.5/PM10/Anomaliler toggle; PM hücresi sağa yatık üç şerit (Q1 üst-sol üçgen / Q2 orta altıgen / Q3 alt-sağ üçgen, her biri EPA konsantrasyon bandı rengiyle), anomali hücresi oran renginde; hover tooltip; veri olmayan/değerlendirilmemiş günler gri; yeni frontend bağımlılığı yok, canlı test edildi — onaylayan: kullanıcı
- [2026-07-08] stage3-patch: SİM (sim.csb.gov.tr) 1 yıllık saatlik geçmiş veri backfill scripti (mqtt/backfill_sim.py) — 11 istasyon eşleştirilip raw_readings+processed_readings+filtered_readings'e yazıldı (99.450 satır), 3 istasyon için yeni stations satırı açıldı (900001-900003, lat/lng NULL, TODO), Kagithane/Uskudar WAQI eşlemesi belirsiz olduğu için atlandı (TODO, script içinde not); EPA breakpoint'leriyle µg/m³→AQI alt-endeks dönüşümü (PM2.5 2024 revizyonu, SO2/NO2/O3 ppb dönüşümüyle), mevcut compute_baseline/filter_reading/process_reading/category fonksiyonları import edilerek kullanıldı (kopyalanmadı); idempotent (aynı station_id+measured_at varsa atlanır), --dry-run ile test edildi, tüm doğrulama adımları (satır sayısı, timezone/yuvarlama, elle AQI hesabı, ilk 24s is_anomaly=NULL, takvim heatmap'te geçmiş veri) geçti; requirements.txt'e openpyxl+et_xmlfile eklendi (pandas gerekmedi), data/ .gitignore'a eklendi — onaylayan: kullanıcı
- [2026-07-08] stage3-patch-devam: Kagithane/Uskudar WAQI eşlemesi çözüldü — WAQI city.url slug'ında "mthm" geçmesi ile Excel'deki dahili istasyon adının "-MTHM" son ekinin eşleşmesi kesin ayraç olarak kullanıldı (8154, 8161), her iki istasyon da backfill edildi (raw+processed+filtered toplam 116.813 satır) — onaylayan: kullanıcı
- [2026-07-08] stage3-patch-not: "Mobil 2" (station_id=14843) seyyar istasyon kısıtı kullanıcı onayıyla olduğu gibi bırakıldı — geçmiş backfill verisi (7815 satır) haritada/heatmap'te stations tablosundaki tek sabit (muhtemelen zamanla değişmiş) konumda gösterilecek, takvim/zaman serisi grafiklerini etkilemez; mqtt/backfill_sim.py ve frontend/CLAUDE.md'ye not düşüldü — onaylayan: kullanıcı
- [2026-07-09] patch: taban harita (tile layer) OSM standart'tan CartoDB Voyager'a değiştirildi (su mavi/yeşil alan/ana yol vurgusu net, mahalle etiket yoğunluğu düşük), subdomains="abcd" + maxZoom=19, attribution OSM+CARTO korundu; API key gerekmiyor — onaylayan: kullanıcı
- [2026-07-10] stage3-patch: İlçe choropleth katmanı (39 İstanbul ilçesi, `frontend/public/istanbul-ilceler.geojson` — kaynak kullanıcı onayıyla `sahircansurmeli/istanbul-geojson`'a değiştirildi, çünkü orijinal önerilen kaynakta ilçe sınırı yoktu, sadece il; name alanı eklenip 780KB'a indirildi); `districtMapping.js` (STATION_DISTRICT_MAP + computeDistrictAQI, ortalama AQI), mevcut `aqiColor()` renk skalası yeniden kullanıldı, veri olmayan 21 ilçe gri (#cccccc); katman marker/heatmap/anomali altında, LayerControl'e "İlçe özeti (choropleth)" eklendi (varsayılan kapalı). Isı haritası etiketi "Isı haritası (tahmini)" oldu, aktifken sol altta interpolasyon uyarısı badge'i eklendi (varsayılan zaten kapalıydı). Yeni bağımlılık yok, Playwright ile canlı ekran görüntüsüyle test edildi — onaylayan: kullanıcı
- [2026-07-10] stage3-patch: Open-Meteo Air Quality API (CAMS model, API anahtarı gerekmez) ikincil katman olarak eklendi — `services/openMeteoService.js` (`fetchOpenMeteoAirQuality`, güncel saat için european_aqi/pm2_5/pm10/no2/ozone döner, hata durumunda null+console log), `hooks/useOpenMeteoData.js` (15 dakikada bir yenileme, unmount'ta interval temizleniyor), `OpenMeteoLayer.jsx` (İstanbul merkezinde tek kesikli CircleMarker, mevcut `aqiColor()` ile renklendirilir, tooltip'te "ölçüm değildir" uyarısı). LayerControl'e "Model AQI (Open-Meteo)" toggle'ı (varsayılan kapalı) + ℹ bilgi ikonu eklendi; katman açıkken sağ altta CAMS/Copernicus atıf kutusu gösteriliyor. Mevcut WAQI akışına/istasyon marker'larına dokunulmadı, veri ayrı katmanda kalıyor; `npm run build` ve canlı API çağrısı (curl) ile doğrulandı — onaylayan: kullanıcı
- [2026-07-10] stage3-patch-devam: Open-Meteo katmanı harita marker'ından widget'a çevrildi — kullanıcı geri bildirimiyle `OpenMeteoLayer.jsx` silindi, yeni `CityAqiWidget.jsx` sağ paneldeki "Canlı AQI Sıralaması"nın üstünde her zaman görünen bir kart (LayerControl'den kaldırıldı, artık toggle'a bağlı değil); büyük renkli AQI rozeti + PM2.5/PM10/NO2/O3 mini istatistikleri + CAMS/Copernicus atıf satırı, `WeatherCard` ile aynı görsel dil. Ayrıca choropleth (ilçe) katmanında poligona tıklayınca tarayıcının çizdiği varsayılan focus dikdörtgeni (Leaflet'in erişilebilirlik için path'lere eklediği tabindex kaynaklı) `.leaflet-interactive:focus { outline: none; }` ile kaldırıldı. `npm run build` ile doğrulandı, kullanıcı tarayıcıda canlı test etti — onaylayan: kullanıcı
- [2026-07-10] stage3-patch (aşama 2a, rüzgar): `leaflet-velocity` bağımlılığı onaylanıp eklendi (yeni bağımlılık kuralı gereği önce soruldu). Open-Meteo Wind API'den Marmara Bölgesi bbox'u (kuzey 41.5°/güney 39.8°/batı 26.0°/doğu 30.5°) için gerçek 3x3 grid (9 nokta, Open-Meteo'nun çoklu konum desteğiyle tek istekte) çekilip leaflet-velocity'nin U/V bileşenli GRIB formatına dönüştürülüyor — `utils/windUtils.js` (`WIND_GRID`, `parseWindData`, meteorolojik yön→U/V dönüşümü), `services/windService.js` (`fetchWindData`), `hooks/useWindData.js` (30 dakikada bir yenileme), `WindLayer.jsx` (`L.velocityLayer`, istasyon marker'larının üstünde, sol altta kendi hız göstergesi). Not: ilk denemede tek nokta (nx=1,ny=1) grid ile interpolasyon imkansız olduğu fark edildi (leaflet-velocity nx-1/ny-1'e bölüyor), bu yüzden gerçek çoklu nokta grid'ine geçildi; ayrıca ilk parametre setinde (maxVelocity=20, soluk renk paleti, düşük particleMultiplier) rüzgar neredeyse görünmezken, sonraki turda aşırı yoğunluk/opaklık haritayı tamamen kapattı — gerçekçi maxVelocity=8 (İstanbul/Marmara tipik rüzgar hızına göre), particleMultiplier=1/200, opacity=0.85, lineWidth=1 ile dengelendi. LayerControl'e "Rüzgar Akışı" toggle'ı eklendi (varsayılan **açık**, ℹ tooltip'te Open-Meteo atfı var, ayrı atıf kutusu eklenmedi). Ayrıca kullanıcı geri bildirimiyle iki yan düzeltme: (1) sağ panel artık flex sibling değil, `position: absolute` + `transform: translateX` ile haritanın üstünde yüzen bir overlay — panel aç/kapa artık harita genişliğini hiç etkilemiyor (önceden flex reflow harita tile'larının bir kısmının boş kalmasına yol açıyordu). `npm run build` ile doğrulandı, kullanıcı tarayıcıda canlı test etti (kapsama alanı, görsel yoğunluk, panel akışkanlığı) — onaylayan: kullanıcı
- [2026-07-10] stage3-patch (aşama 2b, 3D görünüm + state senkronizasyonu): Deck.gl hiç kurulu değilken yönerge onun var olduğunu varsayıyordu (kullanıcıyla doğrulandı) — bu adımda gerçekten kuruldu. 4 yeni bağımlılık onaylanıp eklendi: `deck.gl`, `react-map-gl`, `maplibre-gl` (3D için, token gerekmeyen CARTO voyager-gl vektör stiliyle — 2D taban haritasıyla aynı aile), `zustand` (state store). `lib/aqiUtils.js` eklendi (getAQIColor/getAQIColorRGB/getAQICategory/getAQIBreakpoints — EPA eşiklerinin artık tek kaynağı); mevcut `lib/aqi.js` bu dosyaya delege eden ince bir wrapper'a çevrildi (9 mevcut import noktası dokunulmadan çalışmaya devam ediyor). `lib/stationUtils.js` (`normalizeStation`, `groupStationsByDistrict`) ve `lib/geoUtils.js` (`leafletToViewState`/`viewStateToLeaflet`) eklendi; `districtMapping.js` da `groupStationsByDistrict`'e delege ediyor. `store/mapStore.js` (zustand) — `activeView`, ortak `viewState` (deck.gl formatı kaynak-of-truth), `selectedStationId`, `activePollutant`, `layers`+`toggleLayer`; `LayerControl.jsx` artık prop almıyor, doğrudan store'dan okuyor (App.jsx'teki local `layers` state'i kaldırıldı). `ViewSwitcher.jsx` (sol üstte 2D/3D anahtarı) ve `DeckGLMap.jsx` (yeni 3D görünüm: GeoJsonLayer ile ilçe zemini + ColumnLayer ile istasyon sütunları, yükseklik=AQI, renk=`getAQIColorRGB`, seçili istasyon beyaza dönüyor) eklendi. `StationMarker.jsx` (2D) ve `DeckGLMap.jsx` (3D) artık ikisi de `normalizeStation` üzerinden aynı şekli okuyor; tıklamada `DetailPanel`'in ihtiyaç duyduğu ham `reading` (3D'de `rawById` map'iyle) korunuyor. 2D↔3D geçişte merkez/zoom `viewState` üzerinden korunuyor. Bug: ilk denemede `react-map-gl`'den import edilen `Map` bileşeni JS'in yerleşik `Map` sınıfının üzerine yazıp `new Map(...)` çağrısını (rawById için) kırdı ("Map is not a constructor", beyaz ekran) — import `MapLibreMap` olarak yeniden adlandırılarak düzeltildi. Kullanıcı geri bildirimiyle: eski heatmap "interpolasyon tahminidir" uyarı kutusu kaldırıldı (App.jsx + index.css), rüzgar partikül yoğunluğu 1/200'den 1/300'e düşürüldü. `npm run build` ile doğrulandı — onaylayan: kullanıcı
- [2026-07-10] stage3-patch (aşama 2c, hexbin): İstasyon yoğunluğu için altıgen agregasyon katmanı — gelecekte 100+ sensöre ölçeklenince marker kalabalığını çözer, interpolasyon yapmaz (yalnızca kapsanan istasyonların ortalama AQI'si). Yönergedeki iki paket seçeneği karşılaştırıldı: `leaflet-hexbin` göründüğünün aksine tüm `d3` paketini bağımlılık olarak çekiyor, `d3-hexbin` bağımsız (~137KB) — ikincisi seçildi ve onaylandı. `lib/hexbinUtils.js` (`computeHexbins`) yazılırken yönergedeki örnek koddaki bir hata düzeltildi: orijinal kod `bin.map((_, i) => points[i]?.aqi)` ile bin-lokal indeksi orijinal dizinin global indeksiyle karıştırıp yanlış istasyonların AQI'sini hücrelere atıyordu; bunun yerine `d3-hexbin`'in `.x()/.y()` accessor'larıyla tam `{x,y,aqi}` nesneleri hexbin'e geçirildi, her bin kendi noktalarını doğru taşıyor. `HexbinLayer.jsx` yönergedeki `d3.select` kullanımı yerine (tüm `d3` paketini gerektirirdi) ham DOM/SVG namespace manipülasyonuyla yazıldı — tek bağımlılık `d3-hexbin` olarak kaldı. `LayerControl.jsx`'e "Hexbin (İstasyon Yoğunluğu)" toggle'ı eklendi (varsayılan kapalı); choropleth ile çakışma uyarısı ayrı bir kutu yerine ℹ tooltip'e gömüldü (kullanıcının önceki "haritada fazla uyarı kutusu istemiyorum" geri bildirimine uyularak). İstasyonlar `normalizeStation` üzerinden (aşama 2b'deki ortak şekil) besleniyor. `npm run build` ile doğrulandı — onaylayan: kullanıcı
- [2026-07-13] stage3-patch (aşama 3a, terrain): deck.gl `TerrainLayer` ile 3D görünüme İstanbul topoğrafyası eklendi, yeni bağımlılık gerekmedi (`@deck.gl/geo-layers` zaten `deck.gl` meta-paketinde kuruluydu). Yönergedeki Re:Earth tile endpoint'i test edildiğinde tamamen ölü çıktı (z0/0/0 dahil her istek 404) — AWS Open Data'nın ücretsiz/CORS destekli Terrarium arşifine (`elevation-tiles-prod`) geçildi (curl ile doğrulandı, ücretsiz/hesap gerektirmiyor). Yüzey dokusu önce CartoDB Voyager iken kullanıcı geri bildirimiyle gerçek uydu görüntüsüne (Esri World Imagery, API key gerekmez) çevrildi. Bug: `TerrainLayer`'ın gerçekte bir `elevationScale` prop'u yok (deck.gl kaynağında tanımlı değil, sessizce yok sayılıyordu) — yükseklik abartması slider'ı hiçbir şey yapmıyordu; düzeltme olarak `elevationDecoder`'ın dört katsayısı da (r/g/bScaler+offset) faktörle çarpılıp katmana `id`sine faktör gömülerek (tiled önbellek elevationDecoder değişikliğini otomatik geçersiz kılmadığından) slider değişince tam yeniden yükleme sağlandı. Kullanıcı geri bildirimiyle art arda düzeltmeler: (1) sütunlar çok büyüktü, radius 400→150m elevationScale 80→40'a düşürüldü; (2) 3D harita sınırsız dünya geneli pan/zoom yapabiliyordu — `lib/geoUtils.js`'e paylaşımlı `TURKEY_BOUNDS`+`clampToBounds()` eklendi (2D'nin `MapView.jsx`'teki sınırıyla aynı, kod tekrarı olmasın diye ortak fonksiyona taşındı), deck.gl controller'a minZoom=6 eklendi; (3) terrain açılınca takılma oluyordu — sebebi z14/15'te sürekli tile/mesh yeniden üretimiydi, TERRAIN_MAX_ZOOM önce 13'e düşürüldü (sonra kullanıcı çözünürlük şikayeti üzerine, pan artık Türkiye'ye kilitli olduğundan güvenle 15'e - AWS arşivinin gerçek tavanı, z16+ 404 - çıkarıldı); (4) varsayılan maxPitch 60° iken 85'e çıkarıldı ama CartoDB/MapLibre taban haritası kendi ayrı maxPitch'inde (60°) kalıp senkron bozuluyordu — `MapLibreMap`'e `maxPitch={85}` eklenerek eşitlendi; (5) ufkun üstü çıplak beyazdı, statik mavi→beyaz gökyüzü gradyanı eklendi (`.map-container-sky`, deck.gl'de hazır atmosfer katmanı yok). Bilinen sınırlama (çözülmedi, kullanıcı onayıyla kabul edildi): belli bir zoom-out seviyesinden sonra uydu dokusu bulanıklaşıyor — kaynağı `TerrainLayer`'ın `SimpleMeshLayer`'ın desteklediği `textureParameters` (mipmap/filtreleme) prop'unu kendi `renderSubLayers()`'ında iletmemesi, deck.gl'in kendi API boşluğu; `frontend/CLAUDE.md`'ye not düşüldü. `npm run build` ile her adımda doğrulandı — onaylayan: kullanıcı
- [2026-07-14] stage3-patch (3D yüzey dokusu + gölgeleme düzeltmesi): Kullanıcı isteğiyle 3D terrain yüzey dokusu Esri World Imagery'den Esri World Topo Map'e çevrildi (`DeckGLMap.jsx` `SURFACE_TILE_URL`, aynı `{z}/{y}/{x}` Esri REST şeması — kod değişikliği tek satır), `LayerControl.jsx`'teki katman bilgi metni güncellendi. Ardından kullanıcı yeni dokunun "karanlık" göründüğünü bildirdi — kaynağı araştırıldı: sorun doku değil (Esri tile'ları curl ile indirilip incelendi, düz/pastel renkli), deck.gl'in `TerrainLayer`→`SimpleMeshLayer`'a uyguladığı varsayılan Phong gölgelemesiydi. İlk denemede `material: false` verildi ama etkisiz çıktı — kaynak kod incelendiğinde (`@deck.gl/core/lighting-effect.js`) `layer.props.material`'ın doğrudan `phongMaterial` shader modülüne geçirildiği, modülün `{...defaultUniforms, ...props}` ile birleştirdiği ve JS'te `{...false}`'ın no-op olduğu görüldü — yani varsayılan `ambient:0.35/diffuse:0.6` hep aktif kalıyordu (ışığa ters yüzeyler ~%35 parlaklığa düşüyordu). Doğru kapatma yolu `material: { unlit: true }` olarak bulundu ve denendi (doku çıplak/gölgesiz basıldı) ama bu kez kullanıcı yükseklik farklarının fark edilmediğini bildirdi — son karar: gölgeleme geri açıldı, `material: { ambient: 0.6, diffuse: 0.6, shininess: 1, specularColor: [0,0,0] }` ile genel karanlık azaltılıp (ambient yükseltildi, specular kapatıldı) eğime bağlı diffuse kontrastı (yükseklik okunabilirliği) korundu. `npm run build` her adımda doğrulandı — onaylayan: kullanıcı
- [2026-07-13] stage3-patch (istasyon+CAMS füzyon ısı haritası): Kullanıcı Berkeley Earth'ün hava kalitesi haritasını (berkeleyearth.org/air-pollution-overview) referans göstererek uydu/model verisinin haritalarda nasıl kullanıldığını sordu; cevapta çoğu tüketici haritasının istasyon+model/uydu füzyonu olduğu, saf CAMS'ın tek nokta yerine ızgaraya çevrilebileceği anlatıldı. Önce Türkiye geneline yayılmış 60 noktalık saf CAMS ızgarası (`camsGridService.js`) + `leaflet.heat` ile denendi — iki sorun çıktı: (1) nokta-yoğunluğu (kernel density) tekniği düzenli ızgarada birbirinden ayrık "yaprak" deseni üretti (kaynak kod incelendi, `simpleheat`'in radius sabit piksel, zoom'a göre ölçeklenmiyor); bilinear-interpolasyonlu canvas overlay'e (`ImageOverlay`) geçildi, düzeldi ama (2) CAMS bu ölçekte (tüm Türkiye) İstanbul'un yerel farklarını göremediğinden neredeyse düz tek renk çıktı — bu bir render hatası değil, modelin gerçek çözünürlük tavanı. Çözüm: ızgara Marmara'ya daraltıldı (rüzgar katmanıyla payaşılan `MARMARA_BOUNDS`, `lib/geoUtils.js`'e taşındı — üç yerde tekrar tanımlanmasın), 8x6=48 nokta; `lib/idwInterpolation.js` (saf IDW/ağırlıklı ters mesafe fonksiyonu) ile 21 istasyonun gerçek okuması (ağırlık=25) ve CAMS ızgarası (ağırlık=1) TEK yüzeyde birleştirildi — istasyona yakınken ölçüm baskın, istasyonsuz bölgelerde (Bursa, Tekirdağ vb.) model devreye giriyor. Kullanıcı geri bildirimiyle: iki katman (istasyon HeatLayer + yeni füzyon) aynı anda açıkken taban harita bastırılıyordu, füzyon katmanının opaklığı 0.55→0.4'e düşürüldü; renk uyumu için `lib/aqiUtils.js`'e `getAQIColorSmoothRGB()` eklendi (HeatLayer'ın gradyan durak noktalarıyla aynı `AQI_BREAKPOINTS`'ten türetilen yumuşak geçiş, önceki kesikli/kategorik renk yerine). Ara adımda kullanılan `lib/gridInterpolation.js` (saf bilinear ızgara, IDW füzyonuyla yer değiştirdi) kullanılmayan kod olarak silindi. Katman adı "İstasyon + Model Isı Haritası (Füzyon)" olarak güncellendi, varsayılan kapalı. Kullanıcı ile mutabık kalındı: kalan "izole ada" görünümleri (örn. Şile) IDW'nin doğal karakteristiği, istasyon sayısı arttıkça kod değişmeden iyileşecek. `npm run build` ile her adımda doğrulandı — onaylayan: kullanıcı
- [2026-07-17] stage-docker-1: Proje Windows'tan Ubuntu'ya taşındı, git repo başlatıldı, GitHub'a bağlandı — onaylayan: kullanıcı
- [2026-07-17] stage-docker-2: Docker iskelet yapısı oluşturuldu (10 servis, aqi-network, placeholder Dockerfile'lar, mosquitto/timescaledb config) — onaylayan: kullanıcı
- [2026-07-17] stage-docker-3: TimescaleDB init SQL şemaları oluşturuldu (01_aqi_schema.sql: stations/raw_readings/processed_readings/filtered_readings + hypertable, 02_energy_schema.sql: households/electricity_readings/gas_readings/energy_window_aggregates/energy_anomalies + hypertable) — onaylayan: kullanıcı
- [2026-07-17] stage-docker-4: Python servisleri containerize edildi (aqi-publisher, aqi-subscriber — mqtt/Dockerfile, .env güncellendi: DB_NAME=aqi_db, DB_USER=yusuf) — onaylayan: kullanıcı
- [2026-07-17] stage-docker-5: Backend (FastAPI) ve Frontend (React+Vite) containerize edildi. Multi-stage nginx Dockerfile, /api proxy, .dockerignore, react-router-dom eklendi — onaylayan: kullanıcı
- [2026-07-17] stage-docker-6: Energy-demo servisleri ana projeye entegre edildi (flink-taskmanager, flink-job, energy-publisher). 11/11 container ayakta, tek Docker Compose stack — onaylayan: kullanıcı
- [2026-07-17] stage-docker-7: households CSV verisi energy_demo DB'sine yüklendi (5 hane, idempotent script) — onaylayan: kullanıcı
- [2026-07-17] stage-docker-8: Energy dashboard eklendi (/energy rotası) — hane elektrik tüketim grafiği, anomali kartları, AQI vs elektrik korelasyon grafiği. Backend'e 4 energy endpoint eklendi — onaylayan: kullanıcı
- [2026-07-17] stage-ui-fix-1: Energy dashboard hane elektrik tüketim grafiği yeniden tasarlandı — cihaz bazlı çizgi grafik yerine saatlik toplam tüketimi gösteren dolgulu (fill:true, tension:0.4) mavi (#3b82f6) area chart, x ekseni HH:MM. Backend `/energy/readings/{household_code}` `time_bucket('1 hour', measured_at)` ile sum(consumption_kwh) döndürecek şekilde güncellendi (yeni şema HourlyConsumptionOut, eski ElectricityReadingOut kaldırıldı). Docker backend+frontend rebuild edilip Playwright ile canlı ekran görüntüsüyle doğrulandı, console hatası yok — onaylayan: kullanıcı
- [2026-07-17] stage-ui-fix-2: Ana dashboard'da sağ üstteki `LayerControl` + eski `Energy →` linki kaldırılıp `MapControlWidget.jsx` olarak sol-alta taşındı (Energy butonu + katlanır "Katmanlar" akordiyonu, 190px genişlik). Ardışık kullanıcı geri bildirimleriyle ince ayarlar yapıldı: 3D görünümde terrain "yükseklik abartması" slider'ıyla çakışmaması için widget'a görünüme göre koşullu `bottom` konumu eklendi (2D'de `bottom:0` tabana oturur, 3D+terrain açıkken `map-control-widget--elevated` ile yukarı kayar, aralarında ~6px boşluk); Energy butonu amber yerine beyaz/siyah/gri-hover yapıldı. Ayrıca leaflet-velocity'nin haritanın altına yazdığı "Rüzgar Direction/Speed" metni kaldırılıp yerine widget grubuna üçüncü bir kart eklendi (dönen ▲ ok + derece + m/s) — veri kaynağı `windUtils.js#parseWindData`'nın artık grid'in orta noktasından da çıkardığı ham hız/yön, `mapStore`'daki paylaşımlı `wind` state'i üzerinden (WindLayer yazıyor, MapControlWidget okuyor). Kullanıcının "kart donmuş" gözlemi incelendi: kod/reaktivite doğru çalışıyor, sebep Open-Meteo'nun saatlik veri çözünürlüğü + 30 dk poll interval'ı (bug değil); WindLayer'daki set/cleanup effect'leri yine de ayrıştırıldı. Her adım Playwright ile canlı ekran görüntüsü ve DOM ölçümüyle doğrulandı, console hatası yok — onaylayan: kullanıcı
- [2026-07-20] fix-flink-jdbc-driver: flink-job container'ı ve flink-taskmanager, tüm sink task'larında "No suitable driver found for jdbc:postgresql://..." hatasıyla FAILED oluyordu, API/energy dashboard veri akışı duruyordu. Sebep: 4 sink sınıfında (`AnomalySink`, `WindowAggregateSink`, `ElectricityReadingSink`, `GasReadingSink`) `DriverManager.getConnection` çağrılmadan önce postgresql driver'ı explicit register edilmiyordu — driver jar'a shade ile gömülü olsa da Flink'in per-job classloader'ı altında `DriverManager`'ın statik `ServiceLoader` taraması driver'ı bulamıyordu. Her 4 dosyanın `open()` metoduna `Class.forName("org.postgresql.Driver")` eklendi; `docker compose build flink-job` + taskmanager/job yeniden oluşturulup canlı test edildi (tüm task'lar RUNNING, SQLException yok, backend `/docs` 200 dönüyor) — onaylayan: kullanıcı
- [2026-07-20] fix-wind-widget: sol-alt rüzgar widget kartı (yön oku + m/s) önce imleç konumuna göre güncellenecek şekilde denendi (mousemove + en yakın 3x3 grid noktası, `windUtils.js#findNearestGridPoint`), ama kullanıcı geri bildirimiyle bu yaklaşımın nearest-neighbor sıçramaları yüzünden "her çevirdiğimde farklı data" gibi tutarsız hissettirdiği görüldü. Karar: widget kartı tamamen kaldırıldı, `WindLayer.jsx` stage3-patch (aşama 2a) öncesi orijinal haline döndürüldü — leaflet-velocity'nin kendi `displayValues:true` + `displayOptions` (bottomleft, "Rüzgar Direction/Speed" metni) built-in gösterimi geri geldi; `windUtils.js`/`useWindData.js`'deki `center`/`gridPoints`/`findNearestGridPoint` eklemeleri ve `mapStore.js`'teki `wind` state'i tamamen geri alındı (MARMARA_BOUNDS paylaşımlı sınır importu, CAMS füzyon katmanıyla ortak olduğu için korundu). `app.css`'teki kullanılmayan `.widget-wind-card`/`.wind-arrow` kaldırıldı. Ardından leaflet-velocity'nin kendi metni ile `MapControlWidget`'ın sol-alt widget grubu (Energy butonu + Katmanlar akordiyonu) çakıştığı fark edildi — `.map-control-widget`'ın `bottom` değeri `0`'dan `35px`'e çıkarıldı. Her adım `docker compose up -d --build frontend` ile rebuild edilip kullanıcı tarafından tarayıcıda canlı test edildi — onaylayan: kullanıcı
- [2026-07-20] fix-backfill-sim-docker: `mqtt/backfill_sim.py` Docker ortamında çalıştırılamıyordu — `data/` klasörü ne `mqtt/Dockerfile`'da COPY ediliyordu ne de `docker-compose.yml`'de mount edilmişti (container'da `data/sim/` yoktu); ayrıca script `python mqtt/backfill_sim.py` ile çağrılmıştı ama dosya relative import (`from .subscriber.handlers import ...`) kullandığından yalnızca `python -m mqtt.backfill_sim` ile (modül olarak) çalışabiliyordu. `docker-compose.yml`'deki `aqi-subscriber` servisine `./data:/app/data:ro` read-only bind mount eklendi (image rebuild gerekmedi). `--dry-run` ile 14/14 SİM dosyasının doğru istasyonlara eşleştiği doğrulandıktan sonra gerçek çalıştırma yapıldı — Ubuntu'ya taşınırken DB sıfırlandığından `skipped_dup: 0`, 116.620 satır `raw_readings`'e yazıldı (`processed_readings`/`filtered_readings` otomatik işlendi), `SELECT COUNT(*)` ile doğrulandı. Anomali oranının (~%9-11) yüksek görünmesi `mqtt/CLAUDE.md`'de zaten bilinen `baseline_std` floor teknik borcuyla tutarlı, yeni bir hata değil — onaylayan: kullanıcı
- [2026-07-20] fix-mosquitto-log-perm: `mosquitto` container'ı sürekli "Unable to open log file /mosquitto/log/mosquitto.log for writing" hatası veriyordu (broker çalışmaya devam ediyordu, sadece dosya loglama çalışmıyordu). Sebep: host'taki `mosquitto/log/` dizini sahipliği, image içindeki `mosquitto` kullanıcısının uid'i (1883) ile eşleşmiyordu. `docker compose run --rm --user root --entrypoint sh mosquitto -c "chown -R 1883:1883 /mosquitto/log"` ile servisin gerçekte kullandığı mount üzerinden (Windows'tan `\\wsl.localhost\...` UNC yolu üzerinden manuel `docker run` ile denenen ilk düzeltme, compose'un kullandığından farklı bir mount/uid eşlemesine denk geldiği için işe yaramamıştı) sahiplik düzeltildi. Kod/config değişikliği yok, sadece host dizin izni; `docker compose up -d --force-recreate mosquitto` sonrası log dosyasına gerçekten yazıldığı ve tüm client bağlantılarının (aqi-publisher, aqi-subscriber, energy-publisher, flink-job) göründüğü doğrulandı — onaylayan: kullanıcı
- [2026-07-21] stage_aqi_flink: aqi-subscriber (Python) tamamen kaldırıldı, tüm sorumluluklar yeni `aqi-flink-job` (Java, `com.aqipipeline`) servisine taşındı — energy job (`flink-job`) ile birebir aynı pattern (MqttSourceFunction + parser + WindowAggregator + AnomalyDetector), topic `air_quality/#` (publisher.py'dan doğrulandı). Yeni `StationSink` ile `stations` tablosu canlı tutuluyor (eski sahibi subscriber'dı); `aqi_window_aggregates` (5dk tumbling, istasyon başına avg AQI/PM2.5/PM10/max AQI) ve `aqi_anomalies` (EMA α=0.2, WARNING >%30/CRITICAL >%50 sapma, hypertable partition kolonu kullanıcı düzeltmesiyle `detected_at`) tabloları `01_aqi_schema.sql`'e eklendi. `mqtt/subscriber/consumer.py` silindi; `handlers.py` sadece `backfill_sim.py`'nin bağımlılığı olarak korundu (zscore/kategori mantığı), `raw_readings`/`processed_readings`/`filtered_readings` artık canlı akıştan beslenmiyor, sadece backfill ile dolduruluyor — bu bilinçli bir kapsam kararıydı (kullanıcı veri kaybı/duraklamayı kabul etti). `docker compose build` + `up -d --remove-orphans` ile canlı doğrulandı: gerçek WAQI istasyonları (25 stations) + sentetik test istasyonuyla tetiklenen CRITICAL anomali + 5dk pencere kapanışı (aqi_window_aggregates) uçtan uca çalıştı — onaylayan: kullanıcı
- [2026-07-27 14:20] fix-fresh-clone-recovery: Projeye yeni katılan geliştiricinin (harun) makinesinde dashboard boş açılıyordu. Kök neden zinciri: TimescaleDB volume'u `01_aqi_schema.sql`'in eski bir sürümüyle init edilmiş, `aqi_window_aggregates` PK'si `(id)` olduğu için `create_hypertable(...,'window_start')` "cannot create a unique index without the column window_start" hatası vermiş; psql `ON_ERROR_STOP` ile çalıştığından init orada kesilmiş ve `02_energy_schema.sql` hiç çalışmamış → `energy_demo` DB'si yok → energy Flink job her ~15 sn'de bir FAILED olup `restart: on-failure` ile yeniden submit ediliyor → TaskManager `OutOfMemoryError: Metaspace` ile ölüyor (10 restart) → AQI job da beraberinde düşüyor → hiçbir tabloya veri yazılmıyor. Repodaki SQL zaten düzeltilmişti ama init script'leri yalnızca boş volume'da çalıştığından düzeltme uygulanmamıştı; `docker compose down` + `docker volume rm hava-kalitesi-izleme_timescale_data` + `up -d` ile volume sıfırlandı (tablolar boştu, veri kaybı yok). Ardından `data/sim/` (14 xlsx, `.gitignore`'da olduğu için yeni klonda gelmiyor) takımdan alınıp `docker run --rm --network ... -v ./data:/app/data:ro` ile tek seferlik `python -m mqtt.backfill_sim` çalıştırıldı — `--dry-run` ile 14/14 eşleşme doğrulandıktan sonra gerçek çalıştırma: 116.510 satır `raw_readings`, aynı sayıda `processed_readings`/`filtered_readings`, 2025-07-08 → 2026-07-08 aralığı. Kod değişikliği yok, yalnızca ortam kurtarma — onaylayan: harun
- [2026-07-27 16:20] stage_raw_sink (harun'un dalında, main'e alınmadı): harun aynı boşluğu (canlı WAQI istasyonları `raw_readings`'e düşmüyordu) bağımsız olarak fark edip kendi `RawReadingSink.java` + outer-join çözümünü yazmış; main'de aynı gün paralel olarak `491c575` ile zaten çözülmüştü (`RawReadingsSink` + `EXISTS` subquery). İki yaklaşım karşılaştırıldı, main'inki (zaten doğrulanmış, `shared/aqi.py` tekilleştirmesiyle uyumlu) korundu — bu satır sadece harun'un dalındaki çakışan çalışmayı belgelemek için duruyor, kod alınmadı.
- [2026-07-27 16:35] fix-nginx-resolver: `harun` dalından cherry-pick edildi (main'de bu sorun yoktu ama gerçek bir tuzak, faydalı bir fix). `stage_raw_sink` deploy'ında backend container'ı yeniden yaratılınca frontend 502 vermeye başladı ("connect() failed (111: Connection refused) ... upstream: http://172.18.0.7:8000"). Sebep nginx'in sabit hostname'li `proxy_pass http://backend:8000/` yazımını yalnızca açılışta bir kez DNS'ten çözüp önbelleğe alması: backend'in IP'si değişince frontend de restart edilmeden düzelmiyordu (yeni cihazda kurulum yapan herkesin karşılaşacağı bir tuzak). `frontend/nginx.conf`'ta hostname değişkene alındı (`set $backend_host backend;` + `resolver 127.0.0.11 valid=10s ipv6=off;` — Docker'ın gömülü DNS'i), böylece nginx her istekte yeniden çözüyor. `proxy_pass` değişken içerdiğinde eski yazımdaki sondaki slash'in yaptığı `/api/` → `/` kırpması otomatik uygulanmadığından `rewrite ^/api/(.*)$ /$1 break;` ile URI elle kırpıldı (upstream `/readings/latest` görmeli, `/api/readings/latest` değil). Doğrulama (harun tarafından, kendi dalında): ilk deneme yetersizdi (backend yeniden yaratılınca aynı IP'yi aldı), bu yüzden ağa geçici bir container konulup eski IP işgal edildi ve backend gerçekten farklı IP'ye taşındı (172.18.0.10 → 172.18.0.12); frontend hiç restart edilmeden `/api/readings/latest` üç ardışık denemede de HTTP 200 + 24 istasyon döndü, geçici container silindi — onaylayan: harun. main'e alınması: `hava-kalitesi-harun` worktree'sinde `docker compose up -d --build` ile tam stack ayağa kaldırılıp test edildi — backend, frontend, timescaledb, mosquitto, flink job'ları dahil 10/10 container sağlıklı; `/docs` 200, `/readings/latest` canlı WAQI verisi döndürdü, backend container'ı bilerek restart edilip frontend `/api` proxy'sinin 502 vermeden (200) devam ettiği doğrulandı — onaylayan: kullanıcı
- [2026-07-28] fix-schema-composite-pk: `01_aqi_schema.sql`'de `aqi_window_aggregates` ve `aqi_anomalies` tabloları `id SERIAL PRIMARY KEY` ilan ettiği için fresh volume init'te `create_hypertable(...,'window_start')` / `(...,'detected_at')` "cannot create a unique index without the column ..." hatası veriyordu — madde 47'nin (fix-fresh-clone-recovery) kök nedeni. Kritik keşif: bu düzeltme aslında yalnızca `harun` dalında ve harun'un çalışma kopyasında vardı; main'in commit'li sürümü `d6fb495`→`88c9ab7` boyunca hâlâ bozuktu, yani madde 47'deki "repodaki SQL zaten düzeltilmişti" ifadesi main için geçerli değildi (çalışan DB yalnızca harun'un commit edilmemiş kopyasıyla init edildiği için ayakta kalmıştı). Her iki tablonun PK'si composite'e çevrildi: `PRIMARY KEY (id, window_start)` ve `(id, detected_at)` (partition kolonu PK'ye dahil). Çalışan DB etkilenmedi (init SQL yalnızca boş volume'da çalışır; doğrulama: çalışan DB'de `\d aqi_window_aggregates` → PK `(id, window_start)` + 2 child chunk); değişiklik yalnızca commit'li dosyayı çalışan DB'nin gerçeğiyle eşitleyip gelecekteki temiz kurulumları kurtarır — onaylayan: kullanıcı
- [2026-07-28] fix-fresh-install-setup: Yeni cihazda kurulumu engelleyen üç eksik `harun` dalından `main`'e alındı (dosyalar kod tarafına dokunmuyor, yalnızca dokümantasyon ve build ayarı; iki dal artık bu üç dosyada birebir aynı). (1) Kök `.dockerignore`: `backend/Dockerfile` ve `mqtt/Dockerfile` build context olarak repo kökünü kullandığından, ilk `docker compose up` sonrası mosquitto container'ının uid 1883 + 0600 ile oluşturduğu `mosquitto/data/mosquitto.db` sonraki her build'i "checking context: no permission to read from ..." ile kırıyordu; dosya geçici kaldırılıp `docker compose build backend` çalıştırılarak hatanın gerçekten döndüğü, geri konulduğunda "Successfully built" verdiği doğrulandı (Linux'ta kesin, WSL/Windows'ta dosya sahipliği farklı çalıştığı için görülmez). (2) `.env.example`: `DB_PASSWORD` boştu ve postgres imajı boş şifreyle başlamayı reddediyor ("Database is uninitialized and superuser password is not specified" — izole container ile doğrulandı), `DB_USER=postgres` / `DB_NAME=airquality` ise compose'un gerçekte kullandığıyla uyumsuzdu (compose `POSTGRES_DB: aqi_db` sabit veriyor); `shared/db.py` ve `docker-compose.yml` okunarak compose'un bu dosyadan yalnızca `WAQI_TOKEN`/`DB_USER`/`DB_PASSWORD` okuduğu, diğerlerinin yalnızca host'tan script çalıştırırken (örn. `python -m mqtt.backfill_sim`) geçerli olduğu belgelendi. (3) `README.md`: gereksinimler, kurulum, doğrulama komutları, servis/port tablosu, "harita boşsa" adım adım teşhis, opsiyonel SİM backfill (artık zorunlu değil — canlı akış `raw_readings`'i besliyor), sık karşılaşılan sorunlar ve volume sıfırlama prosedürü; içindeki tüm komutlar main'in çalışan stack'i üzerinde test edildi (`docker compose ps` 10/10, `/stations`, `/api/readings/latest`, `/jobs/overview`, `psql` sayımları). Bu adımda ayrıca yerel kurulum main'in çözümüne geçirildi: eski AQI job cancel edilip `aqi-flink-job` ve `backend` main'in kodundan yeniden derlendi — Flink vertex'i `Sink: raw-readings-sink` olarak doğrulandı, backend 24/24 istasyonda `processed` bloğunu dolduruyor (`category` anlık hesaplanıyor, anomali `aqi_anomalies`'ten `EXISTS` ile geliyor, `algo_version: ema_v1`), backend yeniden yaratılmasına rağmen frontend'e dokunulmadan `/api` proxy'si 200 dönmeye devam etti (nginx `resolver` düzeltmesi çalışıyor) — onaylayan: harun
- [2026-07-29] refactor-flink-dedup: İki Flink projesindeki tekrar temizlendi ve sink'lerdeki DDL kopyaları kaldırıldı. (1) `MqttSourceFunction` iki projede de 117 satırdı ve tek farkı bir yorum kelimesi ile `clientId` önekiydi; ayrıca iki `pom.xml` (isim değişkenleri normalize edilince birebir) ve iki `Dockerfile` de kopyaydı. Yeni `flink-common` modülü (`com.hkizleme.flink.mqtt`) + kök `pom.xml` (parent aggregator: ortak property'ler, flink-streaming-java/slf4j bağımlılıkları, compiler ve shade plugin'leri `pluginManagement`'te, `${main.class}` property'siyle) eklendi; job pom'ları 115 satırdan ~65 satıra indi ve yalnızca kendi `main.class`'ı + domain bağımlılıklarını bildiriyor. `clientId` öneki constructor parametresi yapıldı, böylece iki job broker loglarında hâlâ ayrı görünüyor (davranış değişmedi). Docker tarafı: modül dışı bir bağımlılık Docker context'inden erişilemediği için iki Flink servisinin build context'i repo köküne çekildi (`context: .` + `dockerfile: <modul>/Dockerfile`), Dockerfile'lar `mvn -pl <modul> -am` ile reaktör derlemesi yapıyor ve önce yalnızca pom'ları kopyalayıp bağımlılık katmanını koruyor (parent `<modules>` hepsini listelediği için derlenmeyen modülün pom'u da kopyalanmak zorunda). `.dockerignore`'dan `aqi-flink-job/` ve `flink-job/` çıkarıldı — kök context'e geçişle birlikte bu satırlar build'i kırardı; dosyaya bunu açıklayan bir DİKKAT notu eklendi. (2) Beş sink (`RawReadingsSink`, aqi+energy `WindowAggregateSink`, aqi+energy `AnomalySink`) `open()` içinde `CREATE TABLE IF NOT EXISTS` çalıştırıyordu ve bu DDL'ler `01_aqi_schema.sql`/`02_energy_schema.sql` ile birebir tekrar ediyordu (kod yorumları da "kept identical to" diyerek bunu kabul ediyordu). Bu sadece tekrar değil, aktif bir maskeleme riskiydi: bu oturumda init SQL `create_hypertable` hatasıyla kesildiğinde sink'ler `aqi_window_aggregates`/`aqi_anomalies`'i düz tablo olarak (hypertable dönüşümü olmadan) oluşturup init'in başarısızlığını sessizce gizlemişti — `\dt` tabloları gösteriyordu ama `timescaledb_information.hypertables` boştu. DDL'ler ve `CREATE_UNIQUE_INDEX_SQL` sink'lerden kaldırıldı, tek kaynak init SQL oldu; init SQL'deki artık geçersiz "kept identical to the CREATE_TABLE_SQL in ..." yorumları güncellendi. Doğrulama: `docker compose build aqi-flink-job flink-job` ikisi de başarılı; iki job cancel edilip yeniden submit edildi, ikisi de RUNNING; ortak source'un çalıştığı iki yönden kanıtlandı — energy job source vertex'i `write: 48` (publisher 10 sn'de bir yayınlıyor) ve AQI tarafında var olan bir ölçüm bilerek yeniden yayınlanıp source'un `write: 1` verdiği görüldü, `raw_readings` sayısı 116726'da sabit kaldı (yani sink hem kendi DDL'i olmadan init'in oluşturduğu tabloya yazabiliyor hem `ON CONFLICT DO NOTHING` dedup'ı tutuyor). Test mesajı `StationSink` üzerinden `stations` tablosundaki 8773'ü geçici olarak bozdu (isim "test"), WAQI'den gerçek değerler çekilerek geri düzeltildi (Selimiye, 41.003818/29.026914). Kod değişikliğiyle ilgisi olmayan ama bu sırada ortaya çıkan bulgu: `energy_demo.households` tablosu boş olduğu için `ElectricityReadingSink` her okumayı "Unknown household 'HANE_00X', skipping" diyerek atıyor ve `electricity_readings` 0 satır; sebebi `energy/load_households.py`'nin beklediği `db_households.csv`'nin repoda hiç bulunmaması (`data/sim` ile aynı sınıftan bir temiz-kurulum eksiği) — bu refactor'den önce de böyleydi, ayrıca ele alınmalı — onaylayan: harun
- [2026-07-29] fix-rawpayload-households-cleanup: Kalan üç madde kapatıldı. (1) `raw_payload` regresyonu giderildi: `mqtt/CLAUDE.md` bu alanı açıkça koruma altına almasına rağmen ("ileride ham veriye tekrar bakabilmek için önemli"), subscriber Python'dan Java Flink job'a taşınırken sessizce düşürülmüştü — `RawReadingsSink` 13 kolon yazıyor, `station_name` ve `raw_payload` her satırda NULL kalıyordu. WAQI'nin gönderdiği ham JSON hiç saklanmadığı için parse mantığında sonradan bulunacak bir hata ya da bugün okunmayan bir WAQI alanı geçmişe dönük düzeltilemezdi. `AqiReading`'e `rawPayload` alanı, `AqiReadingParser`'a ham `raw` string'inin geçişi eklendi; sink'in INSERT'ü 15 kolona çıkarıldı (`?::jsonb` ile). (2) `energy_demo.households` boş olduğu için `ElectricityReadingSink` her okumayı "Unknown household 'HANE_00X', skipping" deyip atıyor ve `electricity_readings` 0 satırda kalıyordu; sebebi `energy/load_households.py`'nin beklediği `db_households.csv`'nin repoda hiç bulunmamasıydı (`data/sim` ile aynı sınıftan bir temiz-kurulum eksiği). `02_energy_schema.sql`'e `energy/publisher/publisher.py`'deki HOUSEHOLDS listesiyle birebir aynı 5 hanelik idempotent seed eklendi, böylece demo kutudan çıktığı gibi çalışıyor. Seed eklenince ortaya çıkan tuzak da kapatıldı: `load_households.py` `ON CONFLICT DO NOTHING` kullandığından gerçek CSV eldeyse seed satırlarını hiçbir zaman geçemez, sessizce yok sayılırdı — script `DO UPDATE SET` (upsert) yapacak şekilde değiştirildi, artık CSV seed'in boş bıraktığı zengin alanları dolduruyor (rowcount artık ekleme+güncelleme saydığı için çıktı metni de düzeltildi). (3) `docs/prompts/adim-01-hane-populasyonu-prompt.md:Zone.Identifier` (Windows NTFS alternate data stream artefaktı, `9b75078` ile yanlışlıkla commit'lenmiş) silindi, `.gitignore`'a `*:Zone.Identifier` eklendi. Doğrulama: seed çalışan DB'ye uygulanıp energy job yeniden başlatıldı (HouseholdLookup `open()`'da bir kez yükleniyor) — "Unknown household" uyarıları tamamen durdu ve `electricity_readings` 0'dan 458'e çıktı, 25 saniyede 46→79 artışı gözlendi; `gas_readings`'in 0 olması hata değil, publisher gazı yalnızca 07/08/13/19/20/21 saatlerinde üretiyor ve test 12:48'de yapıldı. `raw_payload` için var olmayan bir `measured_at` ile test mesajı gönderildi: `station_name=Selimiye`, `raw_payload` dolu ve JSONB olarak sorgulanabilir (`raw_payload->>'dominant'`='pm25', `->>'humidity'`='58'); test satırı sonra silindi, `raw_readings` 116726'da sabit kaldı ve `stations` tablosu bozulmadı. Bu doğrulama sırasında bir tuzak yeniden görüldü ve kayda geçiriliyor: `restart: on-failure` + `flink run` kombinasyonunda container'ın eski bir örneğinin submit ettiği job slot'u tutmaya devam ediyor, yeni container ikinci kopyayı submit edemeyip `NoResourceAvailableException` ile sonsuz döngüye giriyor ve cluster ESKİ jar'ı çalıştırmaya devam ediyor — ilk `raw_payload` testi bu yüzden NULL döndü, öksüz job cancel edilince yeni jar devreye girdi. Rebuild sonrası mutlaka eski job cancel edilmeli — onaylayan: harun
- [2026-07-29] fix-flink-job-resubmit: Flink job deploy'unun sessizce eski kodu çalıştırmaya devam etmesi sorunu giderildi. Sorun: compose'daki `flink run` (attached) + `restart: on-failure` ikilisinde container yeniden yaratıldığında cluster'daki ESKİ job çalışmaya devam edip slot'u tutuyor, yeni container ikinci bir kopya submit etmeye çalışıp `NoResourceAvailableException: Could not acquire the minimum required resources` alıyor, exit ediyor ve restart politikası bunu sonsuza kadar tekrarlıyordu. Sonuç sinsiydi: `docker compose build` + `up -d` yapılmasına rağmen cluster eski jar'ı çalıştırmaya devam ediyordu ve dışarıdan her şey normal görünüyordu (job "RUNNING", container "restarting"). Bu tuzak oturum boyunca üç kez ısırdı; en somutu `raw_payload` doğrulamasının NULL dönmesiydi — jar açılıp içindeki INSERT'ün doğru olduğu görüldüğü hâlde, çalışan job container'dan eski (job 09:48:50, container 09:50:12) olduğu için eski kod çalışıyordu. Ayrıca oturumun başındaki TaskManager `OutOfMemoryError: Metaspace` çöküşünün mekanizması da buydu (durmadan yeniden submit → sürekli class yükleme). Çözüm: ortak `flink-common/submit-job.sh` eklendi (iki job da aynı script'i kullanıyor, argüman olarak ana sınıf/jar/job adı alıyor). Script önce bağımlılıkları bekliyor (TimescaleDB ve JobManager TCP portu — `a8d9b4d`'deki hazır-olma beklemesi buraya taşındı), sonra `flink list -r` çıktısından aynı ada sahip çalışan job'ları bulup `flink cancel` ile iptal ediyor, ardından submit ediyor; böylece deploy idempotent oluyor. Ad eşleşmesi `" : <ad> ("` sınırlarıyla yapılıyor ki bir job adı diğerini kapsıyorsa yanlış job iptal edilmesin; `grep` hiçbir şey bulamayınca `set -e` altında pipeline'ı düşürmemesi için `|| true` kullanıldı. Job'lar checkpoint'li olmadığından (MqttSourceFunction "at-least-effort") iptal/yeniden başlatmada kaybedilen kalıcı durum yok. `docker-compose.yml`'deki iki uzun `bash -c` bloğu tek satırlık `command` dizisine indi, Dockerfile'lar script'i `/app/submit-job.sh` olarak kopyalıyor. Doğrulama: elle hiçbir iptal yapılmadan `docker compose up -d --force-recreate` iki kez çalıştırıldı; her ikisinde de script eski job'ı bulup iptal etti ve yeni job submit edildi (job ID'leri `ee596bdbe631`/`7efd914ba6de` → `a0355e2215ca`/`45f40b61fc45` → `672a9af70749`/`99212260df20` olarak değişti), container'lar restart döngüsüne girmedi, `slots-available: 0 / jobs-running: 2` dengede kaldı. Sonrasında veri akışının bozulmadığı doğrulandı: `electricity_readings` 1281→1298 artmaya devam etti, son 1 dakikada 0 "Unknown household" uyarısı, dashboard 24 istasyon, 10/10 servis ayakta — onaylayan: harun 

* [2026-07-29] energy-01-worktree-migration: Adım 1 (hane popülasyonu üretimi) çalışması
  main worktree'de (`~/hava-kalitesi-izleme`) sürdürülürken, başka bir terminalden main
  branch'e demo hazırlığı amacıyla iki kez `git reset --hard origin/main` çalıştırıldı,
  her ikisinde de commit'lenmemiş/push'lanmamış Aşama A-C dosyaları (Dockerfile, config/*.py,
  docker-compose.yml eklentisi, kök requirements.txt) working tree'den silindi — kök neden,
  iki terminalin aynı fiziksel checkout'u (worktree izolasyonu olmadan) paylaşması. Kayıp
  her seferinde `git reflog` üzerinden dangling commit olarak kurtarıldı (son kurtarma:
  `ce87d21`). Kalıcı çözüm: çalışma `git worktree add ../hava-kalitesi-izleme-yusuf yusuf`
  ile main'den tamamen ayrı bir worktree'ye taşındı, `yusuf` branch'i `ce87d21`'den
  türetildi. `data/tuik/` (TÜİK kaynak dosyaları, git'e commit'siz) sembolik link yerine
  kopyalandı (WSL 9P dosya sisteminde symlink güvenilmez davrandı), 4 dosyanın MD5'i main
  ile birebir doğrulandı. `data-generator-dev` container'ı main worktree'den indirilip
  yusuf worktree'sinden yeniden ayağa kaldırıldı (docker-compose mount'ları göreli yol
  olduğu için). Main branch etkilenmedi, temiz kaldı (origin/main = `9b75078`). Bundan
  sonraki tüm Adım 1 çalışması `~/hava-kalitesi-izleme-yusuf`, `yusuf` branch'inde
  yürütülecek — onaylayan: yusuf

* [2026-07-29] energy-01-kentkir-join-fix: `adim-01-hane-populasyonu-prompt-v2.md`'nin
  önerdiği KENT-KIR SINIFLAMASI join yöntemi (`KÖY KAYIT NO.notna()` ile köy/mahalle
  ayrımı) `95.xlsx` üzerinde test edilince çalışmadığı görüldü — `KÖY KAYIT NO` sütunu
  yerleşim tipinden bağımsız olarak TÜM satırlarda dolu (32.254/32.254 mahalle satırında
  da dolu), gerçek bir köy kaydı değil, ayrım için kullanılamaz. Doğru anahtar
  `BELEDİYE/KÖY` sütunu (`'BELEDİYE'`/`'KÖY'` değerleri) olduğu, üç bağımsız sinyalle
  (KÖY ADI notna ⟺ MAHALLE KAYIT NO isna ⟺ BELEDİYE/KÖY=='KÖY') doğrulandı. Türkiye
  geneli test: mahalle join eşleşmeme 0/32.254, köy join eşleşmeme 0/18.183
  (`validate='1:1'` ile). `build_settlements.py` bu yönteme göre yazıldı,
  prompt-v2 §3'e düzeltme notu eklendi — onaylayan: yusuf

* [2026-07-29] energy-01-stage-d-progress: `load_tuik.py` yazıldı, iki bug bulunup
  düzeltildi (_find_col substring eşleşmesi `Zaman`/`Zaman Formatı` ve
  `Gözlem`/`Gözlem Durumu`/`Gözlem Sıklığı` çakışıyordu — tam eşleşme önceliği eklendi,
  5 bilinen boş kolon okuma anında düşürülüyor). Test: Marmara nüfus = 26.710.046 ✓.
  `build_settlements.py` yazıldı (KENT-KIR düzeltmesiyle): 6.370 satır, 0 eşleşmeme ✓.
  `allocate_households.py` yazıldı: Σ hane = 8.529.528 ✓, 11 ilin ölçek değeri ±0,001
  içinde (en büyük fark 0,0004), 11 ilin hane toplamı T06 ile tam eşit, 0-hane guard'ı
  hiç tetiklenmedi (beklenen — TÜİK zaten ≤10 nüfuslu yerleşimleri listeden çıkarmış).
  `assign_attributes.py`: λ değerleri 11 il için prompt-v2 §6.2 referans tablosuyla
  ±5e-5 içinde eşleşti (prompt tablosu 4 ondalık yuvarlanmış olduğundan literal ±1e-6
  anlamsızdı). Örnekleme + konut/ısıtma/çarpan/klima tamamlandı. AÇIK KARAR: doğrulama
  #12 (il bazlı tip dağılımı ±0,001) küçük illerde (Bilecik, Kırklareli) örnekleme
  gürültüsüyle marjinal başarısız oluyordu — tip payını deterministik apportion etmek
  yerine (spec'in "ortak tablodan tek çekim" ilkesini bozacağı için) #12'nin toleransını
  örneklem büyüklüğüne duyarlı yapmaya karar verildi:
  `tolerans_il = max(0.001, 4 * sqrt(p_hat * (1 - p_hat) / n_il))`.
  **Bu formül henüz validate.py'a eklenmedi** — sıradaki iş bu. Kalan: validate.py
  (15 madde), report.py, generate_population.py, load_to_db.py — onaylayan: yusuf

* [2026-07-30] energy-01-validate-report-verify: `~/hava-kalitesi-izleme-yusuf` (branch `yusuf`)
  worktree'sinde load_tuik → build_settlements → allocate_households → assign_attributes →
  validate.py zinciri uçtan uca yeniden çalıştırıldı (Docker Desktop'ın önceki oturumdan
  kapalı kalması dışında bir sorun çıkmadı, container `docker compose -p hkiy up -d --build
  data-generator-dev` ile yeniden ayağa kaldırıldı). validate.py'ın 15 maddelik tam seti
  test edildi: 14/14 uygulanabilir madde GEÇTİ, #11 (parquet bit-tekrarlanabilirlik)
  beklenen şekilde N/A (generate_population.py'ı bekliyor) — 0 KALDI. Önceki turda açık
  bırakılan #12 (il bazlı tip dağılımı, örneklem-duyarlı tolerans `max(0.001,
  4·sqrt(p̂(1-p̂)/n_il))`) dahil tüm maddeler doğrulandı; Bilecik/Kırklareli/Edirne'de daha
  önce marjinal başarısız olan durum artık sorunsuz. Ardından report.py çalıştırıldı,
  `/data/generated/population_report.md` üretildi (9927 byte) — il × dağıtım şirketi
  tablosu (İstanbul AYEDAŞ+BEDAŞ=4.917.759, diğer 10 il tek şirkete bağlı, genel toplam
  8.529.528 ile tam eşit), hane büyüklüğü dağılımı ve tip dağılımı (üretilen vs T06)
  tabloları gözle kontrol edildi, tutarlı bulundu. Kod değişikliği yapılmadı, yalnızca
  doğrulama/regresyon koşusu. Sıradaki iş: `generate_population.py` orkestratörü ve
  `load_to_db.py` — onaylayan: yusuf

* [2026-07-30] energy-01-generate-population: `generate_population.py` orkestratörü
  yazıldı — load_tuik → build_settlements → allocate_households → (ad kolonları için
  il_adi/ilce_adi/belediye_adi/yerlesim_adi kategori sözlükleri yerleşim tablosundan
  türetilip il yazımından önce sabitlendi) → il bazlı (IL_SIRASI sırasıyla, household_id
  sırasıyla aynı) λ-eğilmiş örnekleme + konut/ısıtma/çarpan/klima ataması → pyarrow
  ParquetWriter ile row-group row-group `households.parquet` yazımı. Yarım dosya riskine
  karşı aynı dizinde `.tmp` uzantısıyla yazılıp tüm iller bitince `os.replace()` ile
  atomik taşınıyor (kullanıcı onayıyla, ayrı bir tmp dizini değil — EXDEV riskini önlemek
  için). Bug bulunup düzeltildi: nullable `UInt32` (`belediye_kayit_no`) üzerinde çıplak
  `.to_numpy()` sessizce NaN float64'e dönüştürüyordu (spec'in tam uyardığı tuzak) —
  `.astype(object).to_numpy()` ile pd.NA korunarak düzeltildi, dosyanın gerçek arrow
  şemasında `uint32` olduğu doğrulandı (`dtype_backend='numpy_nullable'` ile okuma
  testiyle). Tek koşu: 8.529.528 satır, 97.8 MB, tepe bellek 3.459,5 MB (<4GB hedefi ✓),
  39,9 sn. Doğrulama #11 (seed tekrarlanabilirlik) kullanıcı onaylı yöntemle test edildi:
  iki ayrı geçici `OUT_DIR`'a (`_repro_test_run1/2`) tam koşu çalıştırılıp
  `validate.py::dogrula_seed_tekrarlanabilirlik` ile sha256 karşılaştırıldı — **GEÇTİ**
  (birebir aynı hash), geçici dizinler try/finally içinde koşu sonucu ne olursa olsun
  silindi. Koşu öncesi `df -h /data` ile disk alanı kontrol edildi (953GB boş, yeterli).
  15/15 doğrulama maddesi artık gerçek anlamda tamamlandı. Sıradaki iş: `load_to_db.py`
  (yeni `households_marmara` tablosu, `COPY FROM STDIN`, indeksler yükleme sonrası) —
  onaylayan: yusuf

* [2026-07-30] energy-01-load-to-db: `load_to_db.py` yazıldı — `households.parquet`'i
  `energy_demo` veritabanında yeni `households_marmara` tablosuna (mevcut 5 hanelik
  `households` tablosuna dokunmadan) `COPY FROM STDIN` ile row-group row-group yükler.
  Tabloda kasıtlı olarak PK/unique constraint yok (household_id benzersizliği
  validate.py #10'da zaten doğrulandı); COPY sonrası 3 indeks oluşturuluyor:
  `(il_kodu, ilce_kayit_no)`, `(dagitim_sirketi)` ve — kullanıcının ileride
  elec_readings/gas_readings hypertable'larının household_id üzerinden join edeceğini
  öngörerek eklettiği — `(household_id)`. `docker-compose.yml`'a yalnızca
  `data-generator-dev` servisine DB env var'ları (`DB_HOST=timescaledb`,
  `DB_NAME=energy_demo` vb.) eklendi, başka hiçbir servise dokunulmadı. Bug bulunup
  düzeltildi: `generate_population.py`'daki ile birebir aynı tuzak —
  `pyarrow.Table.to_pandas()` nullable `UInt32` (`belediye_kayit_no`, köylerde NULL)
  kolonunu sessizce float64'e çeviriyordu, COPY'ye "1394.0" gibi geçersiz tam sayı
  metni giderdi; `.astype('Int32')` ile düzeltildi. Kullanıcı isteğiyle yükleme sonrası
  elle spot-check yapıldı: köy satırlarında `belediye_kayit_no IS NULL` sayısı (127.142)
  köy satır sayısıyla birebir eşleşti, mahalle satırlarında gerçek tam sayı (örn. 52377,
  ondalık yok) doğrulandı, `has_ac` hem `true` hem `false` doğru encode edilmiş NULL yok
  — COPY'nin sessiz NULL/boolean encode hatası riski elenmiş oldu. Test: 8.529.528 satır
  tam eşleşti, eski `households` tablosu dokunulmamış, 3 indeks oluştu, süre 102,3s
  (COPY 80,4s). Test bu worktree'ye özel izole bir Docker volume'ünde (`hkiy_timescale_data`)
  yapıldı, ana üretim ortamı etkilenmedi. Adım 1 (hane popülasyonu üretimi) artık uçtan
  uca tamamlandı: load_tuik → build_settlements → allocate_households → assign_attributes
  → validate.py (15/15) → report.py → generate_population.py → load_to_db.py —
  onaylayan: yusuf
- [2026-07-30] fix-mqtt-reconnect: `MqttSourceFunction` (flink-common, aqi + energy job'larinin ortak kullandigi sinif) broker baglantisi koptuktan sonra kendini toparlayamiyordu. `setAutomaticReconnect(true)` soketi geri getiriyor ama `setCleanSession(true)` yuzunden broker abonelikleri unutuyor ve resubscribe edecek bir mekanizma olmadigindan soket canli / job RUNNING gorundugu halde `messageArrived` bir daha hic tetiklenmiyordu (sessiz olum). Broker her yeniden basladiginda (makine uykusu, `docker compose up`, mosquitto restart) tum akis sessizce duruyor, dashboard eski DB satirlariyla dolu gorundugu icin fark edilmiyordu — bir kez 40 dk olu kalmisti. Cozum: callback `MqttCallback` yerine `MqttCallbackExtended`; yeni `connectComplete(reconnect, serverURI)` yalnizca `reconnect=true` durumunda topic'lere yeniden abone oluyor (`if(!reconnect) return;` ile ilk-baglanti yolu degismiyor, `connectWithRetry` icindeki mevcut subscribe oldugu gibi kaliyor). Her filtre ayri try/catch; `client` referansi callback'e constructor'dan geciyor (her retry yeni client+callback urettiginden sarkan referans yok). Tek sinif degistigi icin iki job da (aqi + energy) birden duzeldi. Dogrulama: `mosquitto` uc kez arka arkaya restart edildi; her seferinde iki job da ~1.3 sn'de "MQTT reconnected, resubscribed" logu basti ve mesaj akisi gercekten dondu (log degil veri: `electricity_readings` 35633->35678 son kayit 7 sn; AQI'ye test mesaji -> source 10->11; `stations`/Selimiye mevcut measured_at ile dedup'landigindan bozulmadi), 10/10 servis + 2 job RUNNING — onaylayan: harun
- [2026-07-30] stage3-aod-layer: Haritaya toggle'lanabilir yeni katman eklendi — NASA GIBS MODIS "Combined Value Added Aerosol Optical Depth" (AOD), PM2.5 icin bilimsel uydu vekili (gece isiklari gibi kentlesme vekillerinin aksine gercek atmosfer olcumu; VIIRS gece-isiklari onerisi bu gerekceyle elendi). Mevcut katman mimarisine sifir surtunmeyle oturuyor, yeni bagimlilik yok: `mapStore.layers`'a `aod:false`, `MapControlWidget.LAYERS`'a bir satir (i aciklamali), yeni `AodLayer.jsx` (react-leaflet `<TileLayer>`), `MapView.jsx`'e kosullu render. GIBS EPSG:3857 "best" endpoint Leaflet ile birebir uyumlu (`GoogleMapsCompatible_Level6`, native max zoom 6, opacity 0.7); tile'lar Leaflet tilePane'inde oldugundan taban haritanin ustunde ama vektor/canvas overlay'lerin ve marker'larin altinda kaliyor. AOD gunluk uretilir ve ~1 gun gecikmeli gelir (bugunun karosu 404, curl ile dogrulandi) — bilesen `Date.now()-2gun` ile kesin veri olan gunu seciyor. Yalnizca 2D gorunumde etkili (hexbin/districts/cams gibi bilincli 2D-only; 3D'de toggle gorunur ama etkisiz). API anahtari gerektirmez (public servis, secret yok). Dogrulama: 3 JSX dosyasi esbuild ile sozdizimsel gecerli, GIBS layer id/projeksiyon/tarih canli curl ile teyit edildi (200 + gercek PNG), harita uzerinde toggle ile aerosol tabakasi goruntulendi — onaylayan: harun
- [2026-07-31] fix-aqi-scale-and-dedup: Kod incelemesinden çıkan dört gerçek kusur giderildi; dördü de ampirik olarak doğrulandı (iddia kabulüyle değil, veriden). (A1) `raw_readings.pm25`/`pm10` sütunları WAQI `iaqi.*.v` değerini tutuyor — bu µg/m³ konsantrasyon DEĞİL, AQI alt-endeksi (0–500). Kanıt: `dominant='pm25'` olan 8/8 canlı satırda `pm25 == aqi` (AQI zaten alt-endekslerin maksimumudur). Oysa `lib/pmScale.js` EPA konsantrasyon eşikleriyle (9 / 35.4 / 55.4 / 125.4 µg/m³) renklendiriyordu, yani alt-endeks 60 ("Orta") 55.4'ü geçtiği için KIRMIZI boyanıyordu — takvim heatmap'i ve PollutantCards kronik olarak fazla kötü görünüyordu. WAQI konsantrasyon vermediği için değerleri gerçek µg/m³'e çevirmek mümkün değil; onun yerine `pmScale.js` ayrı eşik tablosunu bırakıp `lib/aqiUtils.js`'e (haritanın AQI paleti) delege ediyor, `PollutantCards`/`DominantPollutantBox`'taki "µg/m³" etiketi "AQI" olarak düzeltildi. (A2) `CityAqiWidget` Open-Meteo'nun `european_aqi`'sini (EU ölçeği 0-20-40-60-80-100) `aqiColor()` ile yani ABD ölçeğiyle (0-50-100-150-200) renklendiriyordu; EAQI 50 ("kötüye yakın") ABD paletinde yeşil "İyi" çıkıp kirliliği olduğundan iyi gösteriyordu. `openMeteoService` ve `camsGridService` artık `us_aqi` çekiyor — ikincisi ayrıca `CamsHeatmapLayer`'ın istasyon (ABD-AQI) ile model (EU-AQI) değerlerini tek yüzeyde harmanlama sorununu da kapatıyor. Open-Meteo `us_aqi` parametresi canlı test edildi (24 saatlik değer dolu, birim `USAQI`). (A3) WAQI aynı `measured_at`'i her poll'da (5 dk) yeniden yayınladığı için tek fiziksel ölçüm saatte ~12 kez akışa giriyordu. Kanıt: `aqi_anomalies`'te 22 satırın 5'i tekil (%77 mükerrer) ve tek bir ölçüm (9558 @ 10:00, `actual_aqi=15`) 9 anomali satırı üretmiş; `expected_aqi` bu 9 satırda 61.00 → 51.80 → 44.44 → 38.55 → 33.84 → 30.07 → 27.06 → 24.65 → 22.72 diye kaymış — aritmetiği `EMA_ALPHA=0.2` ile birebir tutuyor (0.2×15 + 0.8×61 = 51.8), yani EMA aynı ölçüm tarafından kendi değerine çekilip baseline olma özelliğini kaybediyordu, `severity` de CRITICAL'den WARNING'e düşüyordu. Ayrıca `sampleCountState` de şiştiği için `MIN_SAMPLES_BEFORE_DETECTION=3` eşiği 3 gerçek gözlem yerine ~15 dakikada doluyordu, ve pencere tablosu saatte istasyon başına 12 özdeş satır (`sample_count=1`, aynı `avg_aqi`) üretiyordu. Yalnızca `AnomalySink`'e `ON CONFLICT` eklemek YETMEZDİ: dedup DB yazma katmanında olur, EMA ise stream katmanında zaten bozulmuş olurdu. Bu yüzden yeni `process/MeasuredAtDeduplicator` (istasyon başına `ValueState<Long> lastMeasuredAtMillis`, `millis <= last` ise event düşer) fan-out'tan ÖNCE konumlandırıldı — böylece dört tüketici de (station-sink, raw-readings-sink, pencere, anomali) temiz akış görüyor. `AnomalySink` ek olarak idempotent hale getirildi (`INSERT ... SELECT ... WHERE NOT EXISTS`); `aqi_anomalies (station_id, measured_at)` üzerinde NON-UNIQUE index eklendi (unique olamaz: tablo `detected_at` ile partitionlanmış hypertable, TimescaleDB unique index'in partition kolonunu içermesini şart koşuyor). Checkpoint kapalı olduğundan job restart'ında dedup state sıfırlanır, o an geçerli `measured_at` bir kez daha geçebilir — DB tarafındaki `WHERE NOT EXISTS` + `ON CONFLICT` bunu yakalar (bilinçli iki katmanlı tasarım). (A4) Anomali takvimi yalnızca `aqi_anomalies`'e (canlı EMA) bakıyordu; 1 yıllık backfill anomalilerini ise `filtered_readings.is_anomaly` (zscore_v1) tutuyor. Kanıt: `filtered_readings`'te 11.040 anomali varken `aqi_anomalies`'te 22 satır vardı ve istasyon 8158 için takvim endpoint'i 371 gün / 8783 okuma / **0 anomali** dönüyordu, oysa o istasyonun 963 gerçek anomalisi vardı — stage-3d'nin anomali takvimi özelliği fiilen ölüydü. `services/stations.py#_anomaly_exists_subquery` iki kaynağın BİRLEŞİMİNE (`or_(live, historical)`) bakacak şekilde güncellendi; `filtered_readings (station_id, measured_at) WHERE is_anomaly` partial index'i eklendi. Ayrıca `WindowAggregator` javadoc'u düzeltildi ("event-time" diyordu, iş `TumblingProcessingTimeWindows` kullanıyor — yalnızca yorum hatasıydı). Doğrulama: üç imaj da derlendi; jar açılıp `MeasuredAtDeduplicator.class` ve `AnomalySink`'in yeni SQL'i teyit edildi; Flink boru hattı `Source -> parse-and-filter` / `dedup-by-measured-at -> (station-sink, raw-readings-sink)` / `window-aggregate` / `anomaly-detection` olarak doğrulandı; aynı mesaj 10 kez yayınlanınca `dedup_in` 24→34 arttı ama `dedup_out` 21'de sabit kaldı (10/10 tekrar düşürüldü); backend'in beş endpoint'i 200 döndü, log temiz; istasyon 8158 takvimi 0 → 963 anomaliye çıkıp `filtered_readings`'teki gerçek sayıyla birebir eşleşti; frontend bundle'ında `us_aqi` var, `european_aqi` 0 kez geçiyor. Bilinen kalıntı: mevcut `aqi_anomalies` satırları bozuk EMA ile üretildiği için geçmişe dönük yanıltıcı, temizlenmesi ayrıca değerlendirilecek — onaylayan: harun
- [2026-07-31] cleanup-corrupt-anomalies: `fix-aqi-scale-and-dedup`'ta düzeltilen EMA bozulmasının GEÇMİŞ kalıntısı temizlendi. Dedup operatörü bundan sonrasını temiz tutuyor ama daha önce yazılmış satırlar bozuk baseline'la üretildiği için yerinde kalması yanıltıcıydı — üstelik aynı commit'te takvim/history sorguları artık `aqi_anomalies`'i de okuduğu için bu satırlar canlı döneme ait görünümlere karışıyordu. Tablodaki 25 satırın yalnızca 6'sı tekil (station_id, measured_at) idi ve 6 olayın 5'inde `expected_aqi` aynı fiziksel ölçüm için geniş bir aralıkta geziniyordu: 4146 @ 2026-07-29 08:00 (actual=25) için beklenen 9.0–18.5 ve sapma %35.5–%177.8; 9558 @ 2026-07-29 10:00 (actual=15) için beklenen 22.7–61.0. Okumaları 25–63 bandında olan bir istasyonda EMA'nın 9.0'a çökmesi baseline'ın tamamen bozulduğunu gösteriyor. "Her olayın ilk satırını tut" yaklaşımı seçilmedi, çünkü o ilk satırların baseline'ı da kendinden önceki mükerrerlerle zaten kirlenmişti; yalnızca tek satırlı ve sapmasız olan 4146 @ 07-27 13:00 olayı bundan muaftı, tek başına korunması anlamlı değildi. Silme öncesi güvenlik doğrulaması: `aqi_anomalies`'e FK ile bağlı tablo YOK (cascade riski yok), tabloyu yalnızca backend okuyor (türetilmiş görüntüleme verisi, pipeline yeniden üretir) ve 1 yıllık backfill anomalileri `filtered_readings`'te durduğu için etkilenmiyor. 25 satırın tamamı CSV olarak yedeklendikten sonra `DELETE FROM aqi_anomalies` çalıştırıldı. Sonrası doğrulandı: 5 endpoint de HTTP 200, istasyon 8158 takvimi silme öncesiyle aynı şekilde 963 anomali döndü (asıl değerli veri etkilenmedi), 11/11 servis çalışıyor, 2 Flink job RUNNING, dashboard 24 istasyon. ÖNEMLİ (iki geliştiricili kurulum): `aqi_anomalies` git'te değil, her makinenin kendi Docker volume'ünde duran çalışma zamanı verisidir — bu temizlik yalnızca bu makineyi etkiler, push edilecek bir tarafı yoktur. Diğer geliştiricinin veritabanında da eski kodla üretilmiş bozuk satırlar varsa aynı `DELETE`'i kendi ortamında çalıştırması (ya da volume'ü sıfırlaması) gerekir; `harun` dalını çekmek dedup kodunu getirir ve yeni mükerrer oluşmasını engeller, ama mevcut satırları kendiliğinden temizlemez — onaylayan: harun
- [2026-08-03] fix-hardcoded-db-credentials: Repo ayrımı (outdoor-airq split) öncesi blocker'ı kaldırıldı. `AqiFlinkJob.java` ve `EnergyStreamJob.java` `DB_USER`/`DB_PASSWORD`'ü `getEnv(key, default)` ile okuyor ve varsayılanları GERÇEK kimlik bilgileriydi — yani kullanıcı adı ve iki DB şifresi hem çalışan HEAD kodunda hem git geçmişinde açıkta duruyordu (bkz. `outdoor-airq-repo-split-devir.md` §3.0.1, split'i bloklayan madde). Sırlar için varsayılan kabul edilemez: env verilmediğinde sessizce yanlış kimlikle bağlanmayı denemek yerine açıkça patlaması gerekir, aksi halde yanlış yapılandırma prod'da fark edilmez. Yeni `requireEnv(key)` yardımcısı eklendi (env yoksa/boşsa `IllegalStateException`); `DB_USER`/`DB_PASSWORD` buna çevrildi. Altyapı varsayılanları (`MQTT_BROKER=mosquitto`, `DB_HOST=timescaledb`, `DB_PORT`, `DB_NAME`, `MQTT_PORT`) sır olmadıkları ve compose'la birebir aynı oldukları için `getEnv` ile bırakıldı. Açıklayıcı javadoc'a eski şifre değerleri BİLEREK yazılmadı — hem sırrı yazılı tutmamak hem de split'teki `--replace-text` scrub'ının yorumu bozmaması için. Doğrulama: iki imaj da derlendi; negatif test — `DB_PASSWORD` verilmeden job submit edilince `IllegalStateException: DB_PASSWORD environment variable is required (no default)` ile net biçimde durdu; pozitif test — normal compose deploy'unda iki job da RUNNING, taskmanager'da 0 hata, enerji verisi akmaya devam etti (102845 → 102866). Kalan ilgili iş bu commit'in kapsamı dışında: WAQI token'ın rotasyonu (git geçmişinde `c43a1f9` ve `19fe95b` commit'lerinde açıkta; HEAD temiz) ve geçmişin `git filter-repo --replace-text` ile temizlenmesi — ikisi de split akışında ele alınıyor — onaylayan: harun
- [2026-08-04] ci-ghcr-build-push + fix-batch-image: GHCR workflow'u eklenirken Dockerfile'ın imaja **hiç kod kopyalamadığı** ortaya çıktı — içeriği yalnızca `COPY requirements.txt` + `pip install` + `CMD ["tail","-f","/dev/null"]` idi, yani bind-mount'la içine girilip `docker exec` ile çalıştırılan bir dev kabuğuydu. Bu haliyle GHCR'ye push edilseydi `SYNTHETIC_DATA_SHA` ile pinlenen şey içinde tek satır uygulama kodu olmayan, prod'da `profiles: ["batch"]` altında kalkıp sonsuza kadar `tail` koşan bir imaj olurdu; hata vermeden hiçbir iş yapmazdı. Düzeltildi: `config/`, `src/`, `generate_population.py`, `load_to_db.py` **seçili** kopyalanıyor (`COPY . .` değil — `docs/`, `data/generated/` ve `__pycache__` imaja girmesin), `data/tuik/` (7.6 MB, statik girdi, repoda zaten commit'li) imaja gömülüyor çünkü `src/load_tuik.py:15` girdiyi `/data/tuik` MUTLAK yolundan okuyor ve prod compose'da bu servisin volume'u yoktu. `CMD ["tail",...]` yerine `ENTRYPOINT ["python"]` + `CMD ["generate_population.py"]`; böylece adım değiştirmek argüman meselesi (`docker run <img> load_to_db.py`, `docker run <img> -m src.validate`). `PYTHONPATH=/app` eklendi ki `python -m src.validate` çalışma dizininden bağımsız koşsun. Yanına `.dockerignore` ve eksik olan `.gitignore` eklendi — ikincisi olmadığı için 97 MB'lık `households.parquet` çıktısı ve `__pycache__` commit'e girmeye hazır bekliyordu (`data/tuik/` bilerek ignore'lanmadı: girdi kaynağı, üretimi tekrarlanabilir kılıyor). Workflow tarafında tek servis olduğu için core'daki `changes`+matrix yapısına gerek yok, doğrudan build; `paths` filtresine `data/tuik/**` ve `.dockerignore` eklendi (Dockerfile artık TÜİK girdilerini imaja gömdüğü için girdi değişince imaj çıktısı da değişir, rebuild şart — bu bağımlılık Dockerfile düzeltilmeden önce yoktu), dispatch payload'ı infra'nın yeni sözleşmesine uyacak şekilde `service` → `services` yapıldı (alan adı tutmazsa infra tarafında `SERVICES` boş kalır, deploy sessizce "payload eksik" dalına düşüp SHA'yı güncellemeden `pull && up -d` yapardı), PAT yokluğunda kırmızı yanmaması için guard eklendi, `build-push-action` v5→v6, cache'e scope. Doğrulama iddia değil çalıştırmayla yapıldı: imaj derlendi, `docker run` ile **8.529.528** hane üretildi (beklenen değerle birebir; 97.8 MB parquet, 22.3 sn), `-m src.validate` **15 madde, 15 geçti, 0 kaldı** döndü, imaj içinde `/app` ve `/data/tuik` içerikleri tek tek teyit edildi. Yan bulgu: tepe bellek (`ru_maxrss`) 3.5 GB ölçüldü — `deploy-backlog.md`'deki "VPS ≥4 vCPU / 8 GB" maddesini doğruluyor, bu batch tek başına 3.5 GB istiyor — onaylayan: harun
- [2026-08-06] chore-dev-compose-core-integration: `outdoor-airq-synthetic-data`'da hiç compose dosyası yoktu (yalnızca ad-hoc `docker run` adımları README'de belgeliydi); `outdoor-airq-core`'un dev ortamına (`outdoor-airq-network`, sabit adlı) bağlanan bir `docker-compose.dev.yml` + `.env.example` eklendi — `data-generator-dev` servisi, image'ın `ENTRYPOINT`'i `python` olduğu için `command:` yerine `entrypoint: ["tail","-f","/dev/null"]` ile boşta bekliyor, `networks.outdoor-airq-network.external: true` ile core'un ağına katılıyor. Doğrulama sırasında core'un split-sonrası dev DB'sinin `households_marmara` tablosunu hiç içermediği ortaya çıktı (veri eski monorepo'nun ayrı Docker volume'ünde kalmıştı, split ile taşınmamıştı) — `generate_population.py`'ı yeniden çalıştırmak yerine (üretim deterministik: aynı `data/tuik/` girdisi + aynı `config/`/`src/` kodu → aynı çıktı, ikisi de md5/diff ile birebir doğrulandı) eski monorepodaki `households.parquet`'i (md5 `53317e7c...`) `data/generated/`'e kopyalayıp `docker-compose.dev.yml`'e bir bind mount (`./data/generated:/data/generated`) eklendi — `docker cp` o an Docker Desktop'ta tutarsız davrandığı için (dosya kopyalanmış görünüp exec'ler arasında kayboluyordu) bind mount'a geçildi, ~3.5 GB RAM ve birkaç dakikalık yeniden üretim tasarruf edildi. `load_to_db.py` container içinde çalıştırıldı: 8.529.528 satır, 112.1s, 3 indeks; bağımsız bir `SELECT COUNT(*)` ile ayrıca doğrulandı (8.529.528 = 8.529.528), core'un eski `households` tablosu (5 satır) dokunulmadan kaldı. README'ye "Yerel geliştirme" bölümü eklendi, artık statik olan `outdoor-airq-core_aqi-network` ağ adı referansı güncellendi — onaylayan: yusuf
- [2026-08-12] docs-marmara-restore-path: `households_marmara`'nın kurtarma yolu kırıktı ve bu sessizdi. `docker-compose.dev.yml` `./data/generated:/data/generated` bind mount'u yapıyor, ama bu repodaki `data/generated/` dizini HİÇ YOKTU (`.gitignore:8` gereği parquet git'e girmiyor, split sonrası da kopyalanmamış) — yani bugün `load_to_db.py` çalıştırılsa `FileNotFoundError: /data/generated/households.parquet yok — önce generate_population.py çalıştırılmalı` ile duracaktı. `[2026-08-06] chore-dev-compose-core-integration`'da anlatılan senaryonun aynısı, bu sefer bu repoda. Düzeltme: `households.parquet` (98 MB, md5 `53317e7cc6981c73572b56a144b53c63` — 08-06 kaydındaki hash'le birebir aynı) `data/generated/`'e kopyalandı; dosya gitignore'lu olduğu için commit'e girmiyor, yalnızca çalışma kopyasında duruyor. Ayrıca değerlendirilip REDDEDİLEN öneri: `timescaledb/init/03_marmara_schema.sql` ile tabloyu init'e taşımak — gereksiz, çünkü DDL'in sahibi zaten `load_to_db.py` (`CREATE_TABLE_SQL` satır 87 + COPY sonrası 3 `CREATE INDEX`, satır 106-111); init'e ikinci kopya koymak drift riski yaratır, `down -v` sonrası sadece BOŞ tablo döndürür (veriyi geri getirmez, çünkü `load_to_db.py` yine çalıştırılmalıdır) ve boş tablo "veri var" yanılgısı üretir. Ek olarak dosya `03_grafana_ro.sh` ile numara çakışırdı. Volume kalıcılığı doğrulandı: veri `outdoor-airq-core_timescale_data` named volume'ünde, restart/reboot/`down` silmiyor, yalnızca `down -v`. pg_dump yedeği bilinçli olarak alınmadı: üretim deterministik (`config/seed.py`, `SEED = 20260727`, il bazlı `SeedSequence.spawn`) ve 98 MB'lık parquet, 2.2 GB'lık tablonun dump'ından daha iyi bir yedek artefaktı. Doğrulama iddiayla değil çalıştırmayla: image build edildi ve gerçek bind mount üzerinden KURU bir doğrulama koşuldu (DB'ye hiç bağlanılmadı, `TRUNCATE`/`COPY` çalıştırılmadı — veri zaten yüklüydü) — `load_to_db` modülünün kendi yol çözümlemesi `/data/generated/households.parquet`'i buldu, parquet metadata'sı 8.529.528 satır (= `BEKLENEN_SATIR`), 15 row-group, 19/19 kolon `KOLONLAR` ile eşleşti, row-group 0 (479.651 satır) okunup `_grup_to_csv_buffer` ile COPY'ye giden CSV'ye çevrildi ve ilk satır canlı DB'deki `MARMARA_00000001` kaydıyla tutarlı çıktı. README'nin "Yerel geliştirme" bölümüne `down -v` sonrası geri yükleme reçetesi ve init-şeması önerisinin neden reddedildiği not olarak eklendi — onaylayan: yusuf
- [2026-08-20] adim2b-baslangic: Adım 2b (doğalgaz + katı yakıt kalibrasyonu) başlatıldı, `worktree-adim2b-gaz-kati-yakit` dalı açıldı. Yönerge (`adim-02b-dogalgaz-kati-yakit-yonergesi.md`) `docs/prompts/`'a alındı. Süreç borcu — iki bulgu: (1) `doğalgaz_deepresearch.md`'nin PDF'ten okuduğu 8 parametreli BDEW formülünde (`A/(1+(B/(θ−40))^C)+D+max(m_H·θ+b_H,...)`) `m_H` işareti ters yazılmıştı (Ağustos Ocak'tan yüksek çıkıyordu) — bir daha PDF'ten elle kopyalanmayacak. (2) EPDK "abone" ≠ üretilen "hane" (merkezi ısıtmada sayaç binada, dairede değil) — yönerge §4.4/§4.3'e işlendi, aşağıdaki Karar 1 bunun üzerine kuruldu.

  Yönerge §2'deki altı karardan **beşi kapatıldı, biri (K3) açık kaldı** — kapanmış gibi kaydedilmiyor: **K1** merkezi ısıtmalı haneler gaz yayınlamaz (yalnız kombi, 4.401.560 hane); seviye katmanına `merkezi_pay(il)` düzeltmesi eklendi (İGDAŞ'tan ölçülür, atlanırsa hane başı tüketim %27-46 fazla çıkardı). **K2** kalibrasyon günlük (il×gün, 4.015 satır), saatlik Adım 3b'ye bırakıldı; iki tuzak not edildi (yerel tz gün sınırı, çift normalizasyon zinciri). **K3 — AÇIK:** akış çözünürlüğü (saatlik/günlük yayın) F1 yük testi ölçümüne ertelendi, `masterplan-v4-pipeline-mimarisi.md` §11 satır 9'a da AÇIK olarak işlendi. **K4** `ISITMA_TIPI_ORANLARI` 2b.0'da düzeltilecek ama hedef EPDK/İGDAŞ'tan ölçülerek belirlenecek, ulusal 2,8M rakamına güvenilmeyecek. **K5** katı yakıt kalibrasyon katmanında kapsamda, AQI köprüsü değil; HDD=0 için 0/0 tuzağı ve TÜİK hane-sayısı fit ön koşulu not edildi. **K6 — KAPANDI** Kafka `energy.gas`/`energy.solidfuel` şeması kağıt üzerinde donduruldu, `masterplan-v4-pipeline-mimarisi.md` §10.1'e (payload + dört kural) ve §11 satır 8'e (KAPANDI) işlendi — bu adımda broker/publisher kodu YOK. Masterplan dosyası hiçbir git deposunda değil (`/c/workspaces/mds` versiyonsuz) — ayrı bir sorun, kayda geçsin.

  Faz 2 spike (tek dosya, repo dışı — `src/`'e kalıcı modül yazılmadı) SEKİL testi yaptı, SEVİYE değil: girdi yalnız BDEW sigmoid katsayıları (`demandlib` paketinden — PDF'ten elle kopyalanmadı; SigLinDe DEĞİL, bkz. aşağıdaki yapısal bulgu), Open-Meteo arşivinden 11 il × 2025 gerçek günlük sıcaklık, tek çıpa GAZBİR Marmara Ocak 2025=154,6 m³/hane. **Yapısal bulgu:** `demandlib`'in gerçek formülü `h=A/(1+(B/(θ−40))^C)+D` — yönergenin PDF kaynaklı 8 parametreli `max(m_H·θ+b_H,...)` terimi bu resmi pakette YOK, dolayısıyla planlanan `W` serbest parametresi de yok; tek kategorik seçenek `wind_class` (0/1) kaldı. Adım 0 (işaret testi) geçti (4 profilde de h(6°C)/h(26°C)>1, 8,0-12,4 arası). `wind_class=1` seçildi — gerekçe fiziksel (Marmara Türkiye'nin en rüzgârlı bölgesi, Çanakkale-Balıkesir koridoru + İstanbul Boğazı), bantlara yakın çıkması teyit, seçim sebebi değil. Üç bant sonucu: Ocak/Ağustos=7,996 (hedef [8,10]), ısıtma-dışı pay=%21,7 (hedef [%20,%28], içeride), yıllık=953,0 m³ (hedef [750,950]). Kullanıcı kararıyla **parametre ayarı yapılmadı** (bantlar elle kurulmuş yumuşak ölçütler, IPF zaten Ocak/Ağustos oranını GAZBİR gerçeğine eşitleyecek — spike'ın ölçtüğü yalnızca ay-içi gün dağılımı ve iller-arası pay). İl bazlı 11/11 sonuç yönergenin gerçek maddesi olan [6,14] bandının (madde 12) içinde. **Bilinen sapma, PROGRESS'e kayıtlı, bant içine sokulup unutulmadı:** model mevsimselliği gerçek çıpalardan marjinal olarak daha düz (Ocak payı ~%0,05-0,3 düşük → türeyen yıllık ~%0,3 yüksek); iki ıskalama aynı yönde, muhtemelen aynı kök nedenin iki görünümü — SigLinDe'nin doğrusal su-ısıtma terimi (denendi, reddedildi: işareti düzeltilmiş SigLinDe oranı 4,4-5,4 veriyor, saf sigmoidin 8,0-9,7'sinden daha kötü) yerine sigmoidin kendi D sabitinin gerçek Türk su-ısıtma payını hafifçe fazla tahmin etmesi olası.

  Faz 3 (yerel ortam) yapıldı: `requirements.txt`'e `requests==2.32.3` eklendi; `docker-compose.dev.yml`'e `./data/weather:/data/weather` mount'u eklendi; `.gitignore`'a `data/weather/` eklendi; `households.parquet` (md5 `53317e7c...`, ana checkout'tan) worktree'ye salt-okunur kopyalandı.

  Faz 4 (kısmi — yalnız `config/gas.py` + katsayı CSV'si, henüz `weather_client.py`/`heating_shape.py`/`build_gas_calibration.py` YOK): `data/bdew/bdew_gas_sigmoid_coefficients.csv` `demandlib 0.2.2`'den bir kerelik dışa aktarıldı (ayrı venv, `requirements.txt`'e girmedi); değerler spike'ın h(6)/h(26) sonuçlarıyla birebir çapraz doğrulandı. `config/gas.py`: `GAZ_DAGITIM_MAP` (11 il, EPDK lisans sicili + GAZBİR/şirket sayfalarından web araması ile doğrulandı, deep research raporundan kopyalanmadı), `WIND_CLASS=1`, `SM3_TO_KWH`, HDD/geometrik sıcaklık sabitleri, `HOURLY_GAS_SHAPE` (tanımlı, tüketilmiyor), üç provenance dtype'ı. İl kodları `config/provinces.py` ile 11/11 eşleşti.

  **Yalova (il 77) bilerek `'DOĞRULANACAK'` bırakıldı** — üç kaynak çelişiyor (GAZBİR "Marmaragaz", başka kaynak "Armagaz Arsan Marmara", bir haber 2024 ihalesini "A Doğal Gaz ve Elektrik"in kazandığını söylüyor). Kullanıcı kararıyla üç isimden biri seçilmedi; bunun yerine doğrulama sıkılaştırıldı: `DAGITIM_MAP_BEKLEYEN = {77: 'Yalova'}` + assert'ler — `'DOĞRULANACAK'` yalnızca bu listedeki iller için kabul ediliyor, listede olmayan bir ilde görülürse veya liste büyürse (`len<=1` aşılırsa) `AssertionError`. Gerekçe: bilinen eksik adı-konmuş bir istisna olarak kalmalı, "15/16 de olur" diye normalleşip gerçek bir kırılmayı gizlememeli (Adım 2 madde 9'un sahte pozitifiyle aynı risk sınıfı). Yönerge §6 madde 2 aynı kurala güncellendi.

  **Zaman boyutu ayrı adlandırıldı, çözülmedi:** harita 2026-08-20'de çekilen kaynaklara göre bugünün lisans sahibini gösteriyor, kalibrasyonun referans yılı 2025 — devir yaşanmış illerde (Sakarya AGDAŞ→Aksa, tarihsel Bursa BOTAŞ→Çalık, muhtemelen Yalova) bu ikisi aynı olmayabilir. `GAZ_DAGITIM_MAP_DOGRULAMA_TARIHI="2026-08-20"` ve `KALIBRASYON_REFERANS_YILI="2025"` ayrı sabitler; kabul edilen gerekçe: `gaz_dagitim_sirketi` hiçbir IPF/hesabın girdisi değil, yalnızca payload'a taşınan bir etiket (elektrikteki toplama-anahtarı `dagitim_sirketi`'nin aksine). Yönerge §5 şema tablosuna aynı not işlendi.

  **`bdew_siglinde` artığı için tam tarama yapıldı** (repo + `/c/workspaces/mds`, `grep -rn "siglinde\|SigLinDe\|SIGLINDE"`): iki gerçek yanıltıcı kullanım bulundu ve düzeltildi — `config/gas.py`'da bir yorum başlığı ("demandlib SigLinDe sigmoid katsayıları" → "demandlib BDEW sigmoid katsayıları (SigLinDe DEĞİL)") ve zaten push edilmiş `docs/PROGRESS.md`'deki bir cümle ("BDEW SigLinDe katsayıları" → "BDEW sigmoid katsayıları... SigLinDe DEĞİL"); geçmiş commit amend edilmedi, düzeltme yeni satır olarak eklendi. Yönergede de bir yer ("gerçek SigLinDe uygulaması" diyordu, demandlib'in SigLinDe olmadığını değil olduğunu ima ediyordu) düzeltildi. Kalan tüm geçişler ya açık negasyon ya da Ek A.3/A.4'ün "denenip reddedildi" çerçeveli tarihsel kaydı — kategori değeri `bdew_sigmoid` olarak üç yerde (yönerge, masterplan, config) tutarlı.

  Sıradaki iş: Faz 4'ün geri kalanı (`weather_client.py`/`weather_cache.py` → `heating_shape.py` → `build_gas_calibration.py`) — onaylayan: yusuf

- [2026-08-20] adim2b-veri-cikarimi: Üç kapı (İGDAŞ, EPDK, GAZBİR) tamamlandı, Karar 4'ün kapsamı genişledi. **Kapı 1** (`data/igdas/ilce_kullanim_sinifi_2025.csv`, İBB Açık Veri CKAN, 39 ilçe × MESKEN, 842 satır) ve **Kapı 2** (`data/epdk/il_yillik_tuketim_2025.csv` — Tablo 8.3, TOPLAM sektör; `data/epdk/il_mesken_pay_2022.csv` — 2022 raporunun (gerçek metin DOCX) il×sektör tablolarından 11/11 il için `mesken_pay` ORANI; `data/epdk/il_abone_sayisi_2022.csv` — bağlam, mesken-özel değil) ve **Kapı 3** (`data/gazbir/marmara_aylik_hane_m3.csv`, GAZBİR aylık raporları, **12/12 ay tam** — kısmi çıkarım gerekmedi) commit edildi. GAZBİR'in yıllık Dağıtım Sektörü Raporu (abone_mesken(il) adayı) yalnız taranmış flip-book — OCR/elle transkribe **yapılmadı**, kullanıcı kararıyla kalıcı olarak kapatıldı.

  **Hamle 1** (bedava ölçüm): `mesken_pay(İstanbul) = %61,84` (İGDAŞ mesken tüketimi ÷ EPDK Tablo 8.3 İstanbul toplamı) — ulusal ortalamanın (%35,31) 26,5 puan üstünde, "ulusal oran illere uygulanamaz" tezi sayısallaştı. **Hamle 2**: İstanbul için 2022 oranı (%60,81) 2025'in bağımsız ölçümüne (%61,84) çok yakın çıktı (1,03 puan fark) — oranın yıl-üstü kararlılığını destekledi, bu da "seviye 2025'ten + oran 2022'den" yöntemini (kullanıcı kararı) doğruladı.

  **Karar 4'ün yeni hedefi ölçüldü** (kullanıcı formülüyle, iki bölme): `mesken_tuketim(il) = Tablo_8.3_toplam(il,2025) × mesken_pay(il,2022)`, `ima_edilen_hane_basi = Σmesken_tuketim(11 il)/Σkombi_hane(11 il)`, `duzeltme_carpani = ima_edilen_hane_basi/942,8`. Sonuç: Marmara geneli **1.857,6 m³/hane, çarpan 1,970**; İstanbul tek başına (İGDAŞ gerçek ölçümüyle) **1.975,3 m³/hane, çarpan 2,095**. Çarpan 1'e yakın değil — kombi havuzu gerçekten düşük (Karar 4 madde 3'ün `>1` dalı). Bağımsız bir işaret aynı yönde: görsel okunan (düşük güven) İstanbul konut abone sayısı 5.485.643, modelin İstanbul **toplam** hane sayısından (4.917.759) bile yüksek.

  **Karar 4'ün kapsamı genişledi:** önceki tur "yalnız merkezi payı hedeflenir, soba/elektrikliye dokunulmaz" demişti — kanıtsız bir sınırlamaydı. Artık kanıt var: İstanbul'da doğalgaz erişimi modelin varsaydığından çok daha yaygın, `# VARSAYIM` olan soba (%3,9) + elektrikli (%11, ~730 bin hane) payı gerçekçi görünmüyor. **İstanbul için düzeltme artık tüm ısıtma tipi dağılımını (kombi+merkezi+soba+elektrikli) hedefleyecek**, diğer 10 il için kanıt yok, orijinal kural (yalnız merkezi payı) geçerli kalıyor. Ölçüm henüz `households.parquet`'e uygulanmadı — bir sonraki blok (popülasyon düzeltmesi).

  **Doğrulama bantları gerçek veriyle revize edildi (proxy'den değil):** §6 madde 4, eski PDF-kaynaklı BOTAŞ KFU proxy'si ([8,10]) yerine gerçek GAZBİR serisinden (Ocak/Ağustos=12,47, yıllık=942,8 m³) türetildi; yıllık bandı sıkılaştı ([850,1050], model %1 fark içinde), Ocak/Ağustos bandı madde 12'nin il-bazlı [6,14] bandıyla hizalandı (modelin bilinen düzlüğünü kapsayacak şekilde geniş). **Kayda geçirilen büyütülmüş sapma:** model mevsimselliği gerçek veriden **%36 daha düz** (önceki turda "%0,05–0,3 marjinal" denmişti — bu, ölçülmeden önceki hafife alınmış bir tahmindi, şimdi düzeltildi). Kabul edildi, çözülmedi: IPF sütun marjinali aylık toplamları GAZBİR'e kilitlediği için model şekli yalnızca ay-içi gün dağılımını ve iller-arası payı etkiliyor; kalan artık sapma (soğuk illerin hafif eksik pay alması) ikincil, `# VARSAYIM` — onaylayan: yusuf

- [2026-08-21] adim2b-karar4-sayim-tabanli: Önceki turun tüketim-oranı temelli "düzeltme çarpanı" (~2) yaklaşımı **terk edildi** — kapsama testi (o zamanki madde 19) tutarsız çıktı verdi (%131 kapsama) ve kök neden bulundu: İGDAŞ'ın `kullanım_sınıfı` alanı bina topolojisi değil **cihaz kombinasyonu** tanımlıyor ("MERKEZİ ISINMA+OCAK" bina kazanı değil, merkezi binadaki bir dairenin ocak sayacı). Üç test (kullanıcı tanımlı) bunu ortaya çıkardı: **Test 1** (11 il, kapsama oranı) hatanın tek bir ile değil 6/11 ile dağınık olduğunu gösterdi. **Test 2** (İGDAŞ sınıf başı m³/sayaç) sınıfları ikiye ayırdı: gerçek bina kazanı (`MERKEZİ ISINMA`+`BOYLER`, 24.730 sayaç, 38.320 m³/yıl — bina bandında) vs daire-sayacı-gibi merkezi sınıflar (519.786 sayaç, 76-192 m³/yıl — pişirme bandında). **Test 3** B'nin 1,26 düzeltmesini sorguladı.

  **§4.4.1 KAPANDI:** GAZBİR'in 942,8 m³/hane/yıl'ı **abone** başınadır, hane başına değil — kanıt madde 20'nin abone testi: zincirin ima ettiği İstanbul abone sayısı (5.446.029) İGDAŞ'ın ölçtüğü gerçek değere (5.485.643) %0,76 sapmayla eşleşiyor, tesadüf olamayacak kadar yakın. 1,26 düzeltmesi iptal edildi, yerine ölçülmüş değerler: `kombi dairesi = 869,4 m³/yıl`, `karışım düzeltmesi = 1,084` (=942,8/869,4), ikisi de `level_source=igdas_ilce`.

  **Karar 4'ün İstanbul hedefi artık SAYIM tabanlı, tüketim tabanlı değil:** İGDAŞ'ın sayaç sayıları (kullanım_sınıfı `MERKEZ` içermeyen = kombi, içeren = merkezi) doğrudan oran veriyor — kombi konut birimi %89,5 (4.436.039), merkezi %10,5 (519.786) — modelin İstanbul'da varsaydığı %63,2/%36,8'den **belirgin şekilde farklı**: merkezi payı model'de ~3 kat fazla atanmış, kombi ~%67 eksik. Yan ölçümler: `daire_per_bina` (sayım tabanlı) = **21,0** (önceki turun tüketim-tabanlı 44,08'i terk edildi, bant [20,40] içinde), `ρ` (merkezi/kombi tüketim oranı) = **2,1** (1.823/869,4). Diğer 10 il için İGDAŞ muadili veri yok, Karar 4'ün orijinal kuralı (yalnız merkezi payı) onlarda geçerli kalıyor.

  **Doğrulama listesi güncellendi, iki kez düzeltilerek:** ilk hali (abone sayısını hane sayısıyla birim hatalı karşılaştırıyordu) commit edilmeden yeniden yazıldı, ikinci hali (`kombi_hane+merkezi_hane ≤ toplam_hane`) da **totolojik** çıktı — `isitma_tipi` zaten hanelerin bir bölüntüsü olduğu için mevcut popülasyonla asla başarısız olamaz, hiçbir şey yakalamaz. **Nihai madde 19 — bölüntü testi:** `kombi_hane+merkezi_hane+soba_hane+elektrikli_hane == toplam_hane` (tam eşitlik) + her kategori ≥0. Karar 4 düzeltmesinin en olası uygulama hatasını yakalar: bir kategoriyi çarpanla büyütüp diğerlerini aynı oranda küçültmeyi unutmak (toplam hane sayısı sessizce 8.529.528'den kayar). Düzeltme bir yeniden dağıtım OLMAK ZORUNDA, tek kategoriye ekleme değil. Yeni **madde 20** abone testi (yukarıda, GEÇTİ). Toplam 20 madde. `households.parquet`'e ve `config/gas.py`'a HENÜZ dokunulmadı — popülasyon düzeltmesi ayrı onay bekliyor — onaylayan: yusuf

- [2026-08-21] adim2b-karar4-populasyon-duzeltmesi: Karar 4'ün popülasyon düzeltmesi uygulandı, `households.parquet` YENİDEN ÜRETİLDİ (md5 eski `53317e7c...` → yeni `d5084bb4...`; eski dosya `data/generated/households.parquet.karar4-oncesi-yedek` olarak saklandı, git'e girmiyor). **Yükleme (`load_to_db.py` / `households_marmara`) YAPILMADI — ayrı onay bekliyor.**

  **İki katmanlı düzeltme, kullanıcı formülüyle:** Katman 1 (11 il) — `gaz_payi_hedef(il) = min(1, kapsama(il)/boşluk_faktörü)`, `boşluk_faktörü=5.485.643/4.917.759=1,115476`; her (il,konut_tipi,kent_kir) hücresinde kombi:merkezi oranı VE soba:elektrikli oranı korunarak toplam gaz payı hedefe çekildi. Katman 2 (yalnız İstanbul apartman) — kombi/merkezi/diğer ayrımı İGDAŞ'ın 39 ilçelik gerçek sayımından (`data/igdas/ilce_kullanim_sinifi_2025.csv`, `KOMBİ`→kombi, `MERKEZ`→merkezi, ikisi de yoksa diğer=soba/elektrikli) ilçe×kent_kir düzeyinde türetildi; diğer'in soba/elektrikli bölüşümü o hücrenin eski oranıyla korundu. İstanbul müstakil ve diğer 10 il yalnız Katman 1'i aldı.

  **Kod değişikliği (Karar 4'ün "yalnız ISITMA_TIPI_ORANLARI düzeltmesi" istisnası kapsamında):** `config/housing_profiles.py`'a `ISITMA_TIPI_ORANLARI_IL` (63 girdi, 10 il + İstanbul-müstakil) ve `ISITMA_TIPI_ORANLARI_ISTANBUL_ILCE` (62 girdi, İstanbul-apartman) eklendi; eski tek-tablo `ISITMA_TIPI_ORANLARI` → `ISITMA_TIPI_ORANLARI_ESKI_ULUSAL` (tarihsel referans, artık kullanılmıyor). `src/assign_attributes.py::ata_isitma_tipi` il/ilçe farkındalıklı hale getirildi (yeni imza: `il_kodu`, `ilce_kayit_no` parametreleri eklendi) + savunmacı p-normalizasyonu (numpy'nin `probabilities do not sum to 1` hatasına karşı, 6 ondalıklı yuvarlamadan kaynaklanan hassasiyet kaybı için). Çağrı siteleri güncellendi: `generate_population.py`, `src/validate.py`, `assign_attributes.py`'nin kendi `__main__` bloğu.

  **Karar 5 aynı koşuda: `fuel_type` eklendi.** Yeni `config/solid_fuel.py` (`FUEL_TYPE_KOMUR_ORANI`: YOĞUN/ORTA YOĞUN KENT=0,70, KIR=0,28 — `# VARSAYIM`, TÜİK ulusal kömür payına, %56,1, ±0,1 puan yaklaşacak şekilde elle seçildi), `config/dtypes.py`'a `FUEL_TYPE_DTYPE`, yeni `ata_fuel_type` fonksiyonu (RNG sırasının EN SONUNA eklendi — has_ac'tan sonra — önceki adımların bit-bit korunumunu bozmamak için). Sonuç: 296.564 soba hanenin tamamı dolu (166.307 kömür, 130.257 odun, %56,08 — hedefe ±0,02 puan), soba-dışı 8.232.964 hanede tamamı NULL.

  **Doğrulama — dört ayrı kontrol:**
  1. **Ek A.6 (RNG korunumu) EMPİRİK OLARAK KANITLANDI:** eski/yeni parquet'in TAMAMI satır satır (`household_id` ile eşlenerek) karşılaştırıldı — `base_multiplier` VE `has_ac` **bit-bit aynı** (`np.array_equal` ile, tolerans değil, tam eşitlik). isitma_tipi değişen hane: 3.411.319/8.529.528 (%40).
  2. **Madde 19 (bölüntü testi) 11/11 il için GEÇTİ:** her ilde `kombi+merkezi+soba+elektrikli == toplam_hane`, tam eşitlik, sıfır tolerans; genel toplam 8.529.528 değişmedi.
  3. **Adım 1'in kendi 15 maddelik doğrulama takımı (`src/validate.py`) 15/15 geçti/N-A** (madde 15 dahil: `isitma=='merkezi' => konut=='apartman'` ihlali sıfır).
  4. Öncesi/sonrası tam sayı tablosu il × ısıtma tipi için üretildi (bkz. yönerge Ek A.1) — İstanbul: kombi 2.643.287→4.332.675 (+1.689.388), merkezi 1.541.693→446.446 (−1.095.247); Marmara toplamı: kombi 4.401.560→6.396.834 (+%45), merkezi 2.413.704→1.270.032 (−%47), soba 660.949→296.564 (−%55), elektrikli 1.053.315→566.098 (−%46). Kırklareli'nde (`gaz_payi_hedef=1,0`, tavanlı) soba/elektrikli **tamamen sıfırlandı** — dramatik ama formülün doğrudan sonucu, ayrıca not edildi.

  **Yönerge güncellendi:** Ek A.1/A.2 yeni sayılarla yeniden yazıldı; Ek A.6 empirik kanıtla güncellendi; Ek A.7 (hacim) yeni kombi sayısıyla (6.396.834) yeniden hesaplandı (+%75, eskiden +%52); doğrulama madde 16'nın sabit sayıları güncellendi (4.401.560/660.949 → 6.396.834/296.564).

  **Açık iş, bu turda YAPILMADI, kayda geçirildi:** `config/gas.py::EFH_PAY/MFH_PAY` (%9,34/%90,66) hâlâ Karar 4 ÖNCESİNİN kombi havuzundan — yeni müstakil payı %16,18, belirgin farklı. `build_gas_calibration.py` yazılmadan önce yeniden hesaplanmalı. — onaylayan: yusuf

- [2026-08-21] adim2b-karar4-ab-revizyonu: Karar 4'ün popülasyon düzeltmesi, ilk turun ardından iki kanıta dayalı düzeltmeyle (A, B) revize edildi, `households.parquet` İKİNCİ KEZ yeniden üretildi (md5 `d5084bb4...` → `37c65a0d...`). `households_marmara` yüklemesi HÂLÂ yapılmadı.

  **A — gaz payı tavanı %97 (`GAZ_PAYI_TAVAN`, `config/housing_profiles.py`):** ilk tur Kırklareli'nin soba+elektrikli'sini TAMAMEN SIFIRLAMIŞTI (kapsama/boşluk_faktörü=1,03, tavansız); bu gerçek bir bulgu değil, yöntemin en düşük mesken_pay'e sahip ilde en kırılgan olmasıydı. Aynı tavan İstanbul (0,9928→0,97) ve Kocaeli'ni (0,9799→0,97) de etkiledi.

  **B — kent_kir/konut_tipi yoğunluk ağırlıklı yeniden dağıtım (`DENSITY_AGIRLIK`, `# VARSAYIM`):** ilk tur bir ildeki TÜM (konut_tipi,kent_kir) hücrelerini aynı il-hedefine EŞİT çekiyordu — apartmanla aynı kent_kir sınıfındaki müstakili aşırı gazlaştırdı (müstakil kombi payı modelin genelinde %9,34→%16,18, "%152 büyüme vs apartmanın %34'ü"). B, "şebeke yoğun yerleşime önce ulaşır" ilkesiyle apartman-YOĞUN KENT'e daha büyük pay, müstakil-KIR'a daha küçük pay veriyor — aynı il-hedefi korunuyor, yalnız hücreler arası paylaşım değişiyor. Sonuç: müstakil payı %10,68'e döndü (orijinale, %9,34'e, çok daha yakın — B "eşitleme" değil "ağırlıklandırma", tam eşitlik beklenmiyordu).

  **C — Madde 21 eklendi, kalıcı, hattaki TEK dışa dönük doğrulama:** `Σ_il[(kombi_hane+merkezi_hane+merkezi_hane/daire_per_bina)×boşluk_faktörü] ≈ Σ_il[mesken_tuketim(il)/942,8]`, tolerans ±%5. Sol taraf households.parquet'in ürettiği sayaç-eşdeğerini abone sayısına çevirir (kombi daireleri + merkezi daireleri KENDİ ocak sayaçlarıyla + merkezi binaların PAYLAŞIMLI kazan sayacı, `daire_per_bina=21,0` ile), sağ taraf EPDK/GAZBİR zincirinin bağımsız ima ettiği abone sayısı. **Sonuç: sapma %-2,54, GEÇTİ** (kullanıcının ön-tahmini %0,65'ten farklı ama aynı mertebede — A/B'nin kombi/merkezi dağılımını değiştirmesinden kaynaklanıyor). İl bazında dağınık: Kırklareli tek başına %-22,7 sapıyor (en küçük popülasyon, Kapı 2/3'te de tekrarlayan kırılganlık), toplamda telafi oluyor.

  **D — `config/gas.py::EFH_PAY/MFH_PAY` B'den SONRA yeniden ölçüldü** (sıra kasıtlı, B müstakil payını doğrudan değiştiriyor): %9,34/%90,66 (Karar 4 öncesi) → %10,68/%89,32 (B sonrası) — orijinale çok yakın, B'nin niyet ettiği etkinin dolaylı doğrulaması.

  **`config/solid_fuel.py::FUEL_TYPE_KOMUR_ORANI` da yeniden kalibre edildi** (D'de istenmedi ama aynı sınıf zorunluluk): A/B soba popülasyonunun büyüklüğünü (296.564→486.046) VE kent_kir dağılımını değiştirdi, eski KIR=0,28 artık toplamda %46,3 kömür veriyordu (hedef %56,1'den 9,8 puan sapma). KENT=0,70 sabit tutulup KIR=0,4551'e çözüldü (kapalı form) → %56,1'e tam.

  **Dört doğrulama TEKRAR koşuldu, hepsi geçti:** Ek A.6 (RNG korunumu, orijinal — Karar 4 öncesi — dosyaya karşı bit-bit aynı `base_multiplier`/`has_ac`), madde 19 (bölüntü, 11/11 il tam eşitlik), Adım 1'in 15 maddesi (15/15), fuel_type dağılımı (%56,25 kömür, hedefe ±0,15 puan).

  **Nihai sayılar:** `Σ kombi_hane=6.149.023`, `Σ merkezi_hane=1.363.320`, `Σ soba_hane=486.046`, `Σ elektrikli_hane=531.139` (Ek A.1/A.2/A.6/A.7 ve doğrulama madde 16 yönergede güncellendi). Eski (orijinal, Karar 4 öncesi) yedek `data/generated/households.parquet.karar4-oncesi-yedek`'te duruyor (git'e girmiyor) — onaylayan: yusuf

- [2026-08-21] adim2b-households-marmara-yukleme: `households_marmara` (TimescaleDB `energy_demo`) Karar 4'ün A/B-düzeltilmiş `households.parquet`'iyle (md5 `37c65a0d...`) yeniden yüklendi. **Yükleme öncesi mekanizma doğrulandı:** `load_to_db.py` `TRUNCATE households_marmara` çalıştırıp SONRA `COPY` yapıyor (satır 84) — ekleme değil tam değiştirme, "17 milyon satıra çıkar" riski yoktu. Docker Desktop kapalıydı, başlatıldı; `core-timescaledb-1` container'ı `Exited (255)` durumundaydı (Docker Desktop'ın kendi yeniden başlamasından, veri bozulması değil — loglar temiz kurtarma gösterdi), `docker start` ile ayağa kaldırıldı.

  **Yükleme öncesi/sonrası satır sayısı:**
  - Öncesi: `households_marmara`=8.529.528 (Karar 4 öncesi, orijinal sayılarla — `isitma_tipi` dağılımı Ek A.1'in eski tablosuyla birebir eşleşti), `households`=5 (dokunulmamış).
  - COPY: 15 row-group, 8.529.528 satır, 147,3s. İndeksler + `ANALYZE` sonrası toplam 158,3s.
  - Sonrası: `households_marmara`=**8.529.528** (tam), `households`=**5** (değişmedi).

  **Üç eşitlik doğrulandı, hepsi tam:**
  1. `COUNT(*)` = 8.529.528 ✓
  2. `GROUP BY dagitim_sirketi`: BEDAŞ 3.139.331 · AYEDAŞ 1.778.428 · SEDAŞ 1.011.180 · UEDAŞ 1.842.339 · Trakya EDAŞ 758.250 — **bölge toplamları Karar 4 ÖNCESİYLE birebir aynı** (yönergenin öngördüğü gibi: düzeltme il-içi yeniden dağıtım, bölge toplamlarına dokunmadı — Adım 2 kalibrasyonu ve Adım 3'ün `w_bölge` sabitleri güvende).
  3. `GROUP BY isitma_tipi`: kombi 6.149.023 · merkezi 1.363.320 · soba 486.046 · elektrikli 531.139 — parquet'teki sayılarla birebir.

  **Madde 21 il bazlı genişletildi** (yönergeye işlendi): toplam sapma %-2,54 (GEÇTİ, ±%5 içinde); il bazlı — İstanbul -%2,33, Kocaeli -%6,17, Sakarya -%5,27, Bursa -%0,09, Balıkesir +%1,55, Çanakkale +%0,60, Yalova -%3,60, Tekirdağ +%1,68, Edirne -%1,82, **Kırklareli -%22,70 (UYARI, eşik ±%15 aşıldı)**, Bilecik +%1,74. Kırklareli'nin sapması bilinen ve kabul edilen (%97 tavanının doğrudan sonucu) — toplamın içine gizlenmeden UYARI olarak kalıcı kayıtta duruyor. — onaylayan: yusuf

- [2026-08-21] adim2b-ipf-marjinal-duzeltmesi: §4.3'ün (seviye katmanı, IPF çift marjinal) planı — henüz kod yazılmadan — düzeltildi. **Marjinal 1** artık `Tablo_8.3_toplam(il) × mesken_pay(il) × (1−merkezi_pay(il))` — yalnız kombi tüketimi, "EPDK yıllık il tüketimi" (mesken+merkezi karışık) değil. Gerekçe: Karar 1 yalnız kombi haneler gaz yayınlıyor; paydası kombi_hane olan bir marjinale merkezi tüketimini karıştırmak, Karar 4'te iki kez yakalanan aynı hatanın (mesken_tuketim'e merkezi dahil edilince hane başı %27–46 şişmesi) üçüncü tekrarı olurdu — bu kez kod yazılmadan önce yakalandı. **Marjinal 2** artık GAZBİR'in mutlak seviyesini (942,8, abone-bazlı/karışık popülasyon) DEĞİL, yalnız aylık ŞEKLİNİ (`ay_payi(m)=GAZBİR_ay(m)/942,8`, toplamı 1) kullanıyor, `Σ_il kombi_tuketim(il)` ile çarpılıyor — iki marjinal de artık aynı birimde (kombi-özel m³) ve gerçek veriden, abone/hane belirsizliği IPF'e hiç girmiyor. Kod değişikliği yok (build_gas_calibration.py henüz yazılmadı) — onaylayan: yusuf

- [2026-08-25] adim2b-yonerge-birim-etiketleme: Kod yok, yalnız yönerge düzeltmesi (worktree'ye push öncesi son adım). Üç alt madde:

  **2a — doğrulama madde 4'ün yıllık bandı birim hatasıydı, düzeltildi:** eski bant `[850,1050]` GAZBİR'in **abone başına** 942,8'inden türetilmişti; bu modülün çıktısı ise **kombi hanesi başına**, ve zincirin ima ettiği gerçek değer daha yüksek: `Σ kombi_tuketim/Σ kombi_hane = 6.574.277.830/6.149.023 ≈ 1.069 m³/hane/yıl`. **Yeni bant: [950, 1.200] m³/hane/yıl (kombi hanesi başına, abone başına DEĞİL).** İl bazlı dağılım beklentisi de madde 4'e eklendi: İstanbul (yoğun, ılıman) bandın alt yarısında, Edirne/Kırklareli/Bilecik (soğuk, düşük mesken_pay) üst yarısında olmalı — build_gas_calibration.py çalıştığında bu doğrulanacak, doğrulanmazsa madde şüpheli sayılacak.

  **2b — §4.3'e IPF'in dört satırlık hesap zinciri işlendi:** `tohum(il,ay)=kombi_hane(il)×Σ_{d∈ay}h(θ_il,d)` → `IPF→hücre(il,ay)` (iki marjinali de tutturur) → `gün_ağırlığı(il,d)=h(θ_il,d)/Σ_{d∈ay}h(θ_il,d)` (ay içinde toplamı 1) → `günlük_hane_m3=hücre(il,ay)×gün_ağırlığı(il,d)/kombi_hane(il)`. İki not eklendi: (i) tohum il-özel sıcaklık tepkisini ve EFH/MFH karışımını taşır, marjinaller yalnız SEVİYEyi zorlar, ŞEKLİ değil; (ii) yakınsama kuruluşta garantidir çünkü iki marjinal de aynı toplama (Σ_il kombi_tuketim(il)) eşitlenmiştir.

  **2c — birim etiketleme kuralı geriye dönük uygulandı, tüm belgede taranarak:** kullanıcının koyduğu kural — "gaz tarafında hiçbir sayı, birimi adında veya yorumunda yazmadan durmaz" — daha önce dört kez (Karar 4'ün iki turu, madde 4'ün bandı, IPF marjinal 1) yakalanan abone/hane karışıklığının bir daha görünmemesi için tüm doküman GAZBİR/EPDK/İGDAŞ kaynaklı her sayı için tarandı. Düzeltilen yerler: §4 genel bakış diyagramı (`seviye_zamansal` satırına "m³/abone" + ŞEKİL notu), §4.2.2 spike sonuç cümlesi (154,6 m³'ün abone başına olduğu, spike zamanında henüz kapanmamış ayrım olduğu notu), §4.3 Marjinal 2 çevresindeki 953,0/942,8 karşılaştırması (ikisi de abone başına, ŞEKİL testi olduğu için LEVEL'a taşınmadığı notu), §4.6/madde 14 ölçek kontrolü (154,6 m³/ay'ın abone başına olduğu + elektrikle (hane başına) karşılaştırmanın iki farklı payda karşılaştırdığı uyarısı, düzeltilmiş ~142,6 m³/ay tahmini), Ek A.4 tablosu (başlık "yıllık m³/hane" → "yıllık m³ (çıpa birimiyle: abone başına)", TÜİK 978 m³ çıpasına da abone-başına etiketi + madde 4'ün nihai [950,1.200] bandıyla karıştırılmaması uyarısı), Ek A.5 (154,6'nın abone başına olduğu artık koşullu değil kesin ifadeyle, "114–122 m³/ay, kombi hanesi başına" düzeltmesi). — onaylayan: yusuf

- [2026-08-25] adim2b-il-koordinat-hdd-referans: Kod yok, yalnız girdi hazırlığı — Adım 3'ün ön koşul doğrulaması sırasında `data/weather/`'ın tamamen boş olduğu bulundu (11 il için hiç çekilmemiş). Eksik ikinci girdi: il merkezi lat/lon koordinatları — kodda hiçbir yerde tanımlı değildi. **Koordinat kaynağı, onaylanan disiplinle:** Open-Meteo Geocoding API (`geocoding-api.open-meteo.com/v1/search`), elle transkripsiyon YOK — sıcaklık verisiyle aynı sağlayıcı. Sorgu il ADIYLA değil il MERKEZİ şehir adıyla yapıldı (Kocaeli→İzmit, Sakarya→Adapazarı) çünkü il adıyla arama bu iki ilde alakasız köy kayıtları döndürüyordu. 11 il de tek ve doğru PPLA (idari merkez) kaydına düştü — `config/gas.py::IL_KOORDINAT`'e donduruldu (2026-08-25 tarihli).

  **11 il için weather_client.py/weather_cache.py ile fiilen çekildi:** 2025 tam yıl (365 gün/il, arşiv ucundan) + 2024 kuyruğu (28-31 Aralık, θ_ref'in 3 günlük ısınma penceresi için). `data/weather/{il}_{yıl}.parquet` olarak yazıldı (`.gitignore`'da, git'e girmiyor).

  **Makullük kontrolü — HDD (taban 18°C), yıllık ortalama sıcaklık DEĞİL:** ilk turda "İstanbul/Yalova/Çanakkale en ılık" beklentisi yıllık ortalama sıcaklığa dayanıyordu ve sıralama tutmadı (Edirne ortalamada 7., Yalova beklenenin aksine orta sırada) — kullanıcı bunun üzerine ölçütü HDD'ye çevirdi: Edirne karasal iklim nedeniyle ortalamada 7. ama HDD'de 3. çıkıyor, "kıyı=ılık" sezgisi ortalama sıcaklıkla yanıltıcı. **Ölçülen HDD_2025 (taban 18°C), yönergeye Ek A.8 olarak donduruldu, `config/gas.py::HDD_REFERANS_2025`:** Bilecik 2312 · Kırklareli 1963 · Edirne 1861 · Tekirdağ 1847 · Kocaeli 1764 · Yalova 1695 · Sakarya 1684 · Bursa 1670 · Balıkesir 1637 · İstanbul 1514 · Çanakkale 1469. Marmara aralığı (1469–2312) MGM'nin Türkiye uzun-yıllar ortalamasının (~2191) altında — beklenen davranış (TS 825 iklim bölgesi 2-3). Rakım kontrolü: hiçbiri şüpheli değil (9–517 m, Bilecik'in 517 m'si gerçek — plato ili).

  **Yönergeye yeni doğrulama madde 22 eklendi:** gelecekteki koşularda ölçülen HDD bu referans tablodan `>%20` saparsa UYARI (yanlış koordinat/yıl/bozuk cache ihtimali). Madde 4'ün il-bazlı beklentisinin gerekçesi de HDD'ye netleştirildi (önceden ortalama sıcaklığa dayanıyormuş gibi okunuyordu).

  **θ_ref yıl-sınırı ön koşul testi ayrıca koşuldu ve GEÇTİ** (bu commit'e dahil değil, kod değişikliği yok): 2024 kuyruğu + 2025 verisi birleştirilince `theta_ref(2025-01-01)=6,30` (NaN değil), elle hesapla birebir eşleşti, 29 Aralık'ı bozunca sonuç değişti (2024 verisinin gerçekten kullanıldığının kanıtı), kuyruk verilmeden çağrılınca doğru şekilde NaN döndü (kasıtlı davranış, madde 5). `heating_shape.py` değişikliği gerekmedi. — onaylayan: yusuf

- [2026-08-25] adim2b-build-gas-calibration: `src/build_gas_calibration.py` yazıldı ve koşuldu — `data/generated/calibration_gas.parquet` üretildi (11 il × 365 gün = 4.015 satır, `ilce_kayit_no` YOK, 2026-08-25 kararıyla il düzeyinde; İGDAŞ'ın ilçe verisi zaten Karar 4'te popülasyona işlenmişti, Adım 3b'de ayrı bir ilçe çarpanı olarak yeniden devreye girecek).

  **İlk koşu sonucu ve kabul edilen beş sayı:** satır sayısı 4.015 ✓; Marmara yıllık hane başı `Σkombi_tuketim/Σkombi_hane = 1.069,2` (bant [950,1.200]) ✓; Ocak/Ağustos oranı (Marmara) 12,468 (gerçek GAZBİR 12,47) ✓; IPF 2 iterasyonda yakınsadı, son sapma %0,003 (tolerans ±%0,1) ✓.

  **İlk koşu bir kategori hatasını ortaya çıkardı — madde 4 üçe bölündü (4a/4b/4c):** il bazlı yıllık hane başı tablosu HDD sıralamasıyla uyuşmadı (beklenen üst uç Bilecik/Kırklareli/Edirne yerine gerçek üst uç Kırklareli/Kocaeli/Bursa). Kök sebep: bu değerler (`kombi_tuketim(il)/kombi_hane(il)`) tümüyle EPDK'nın mesken_pay/sektörel tüketim zincirinden gelir — `theta_ref`/HDD yalnız IPF'in ay/gün İÇİ dağılımını (ŞEKİL) belirler, satır marjinali `kombi_tuketim(il)`'e kilitli olduğu için yıllık toplam (SEVİYE) iklimden hiç etkilenmez. Kırklareli'nin en yüksek çıkması düşük `mesken_pay`'inden (%9,74 — tüketiminin çoğu sanayi) kaynaklanıyor, iklimden değil; bu, madde 21'in -%22,70 UYARI'sıyla aynı kırılganlığın ikinci görünümü.

  **Düzeltme: madde 4 → 4a (Marmara toplamı, seviye, [950,1.200], ölçülen 1.069,2 GEÇTİ) + 4b (il bazlı, HDD beklentisi SİLİNDİ, yerine geniş makullük bandı [800,1.700], 11/11 il içeride GEÇTİ) + 4c (HDD beklentisi doğru katmana — ŞEKİL/mevsimsel genlik — taşındı: il bazlı Ocak/Ağustos oranı ile HDD arasında Pearson korelasyonu 0,7132, güçlü pozitif, GEÇTİ; alt uç İstanbul/Çanakkale birebir tuttu, üst uç HDD'nin en yüksek olduğu 3 ilin (Edirne/Kırklareli/Bilecik) hepsi en yüksek genlik grubunda ama aralarındaki sıra HDD sırasıyla birebir örtüşmüyor — h(θ)'nın doğrusal olmayan tepkisiyle tutarlı, beklenen).**

  **Yeni madde 23 eklendi (kalıcı, madde 21 ile birlikte hattaki İKİNCİ dışa dönük doğrulama):** `kombi_tuketim(İstanbul) ≈ 869,4 × 4.436.039` (İGDAŞ'ın bağımsız ölçtüğü kombi dairesi ortalaması × sayacı). Tolerans ±%10 (madde 21'den geniş — iki farklı `kombi_hane` tanımı karşılaştırılıyor: model popülasyonu vs İGDAŞ ham sayaç sayımı). **Ölçüldü: model 4.128.409.138 m³ vs İGDAŞ çapası 3.856.692.307 m³, sapma %+7,05 — GEÇTİ.**

  **Kaydedilecek sınır (madde 4b'nin makullük bandına neden bir dış çapa değil de geniş bant seçildiği):** İl bazlı yıllık hane başı değerlerinden yalnız İstanbul bağımsız doğrulanmıştır (İGDAŞ, madde 23). Kalan 10 il, `mesken_pay(il)` (EPDK 2022) ve İstanbul'dan miras alınan `merkezi_pay` hatalarını doğrudan devralır. Düşük `mesken_pay`'e sahip illerde (Kırklareli %9,74, Kocaeli %16,67) küçük bir oran hatası büyük mutlak sapma üretir — Kırklareli'nin madde 21'deki −%22,70 uyarısı ve bu tablodaki en yüksek değer (1.579,6) aynı kırılganlığın iki görünümüdür.

  Kod değişikliği yalnız yeni dosya: `src/build_gas_calibration.py` (280 satır) — mevcut hiçbir dosyaya dokunulmadı. Bu turda henüz commit edilmedi (girdi hazırlığı ayrı, önceki commit'te (`02233f5`) zaten gitti; bu ikinci commit modül + madde 4a/4b/4c/23 yönerge değişikliklerini birlikte taşıyacak). — onaylayan: yusuf

- [2026-08-25] adim2b-build-solid-fuel-calibration: `src/build_solid_fuel_calibration.py` yazıldı ve koşuldu — `data/generated/calibration_solid_fuel.parquet` üretildi (11 il × 365 gün = 4.015 satır). Gazla aynı makine (sıcaklık katmanı, popülasyon, çıktı deseni) ama üç KASITLI fark: (1) IPF yok — katı yakıtın ne il bazlı ne aylık gerçek marjinali var, zincir tek yönlü (Marmara toplamı gazın doğrulanmış seviyesinden türetilir, il dağılımı yalnız HDD ile — EPDK'nın `mesken_pay` hatasını miras almadan); (2) normalizasyon YIL içinde (AY değil) — aylık çapa olmadığı için, bu aynı zamanda sıfır-HDD/0-0 tuzağını yapısal olarak ortadan kaldırıyor; (3) şekil sigmoid değil HDD (`Tm>15°C` ise 0 eşiğiyle, `config.gas.HDD_BASE_TEMP`/`HEATING_THRESHOLD`).

  **`SOBA_YAKIT_ENERJI_ORANI` fit ÖN KOŞULU SAĞLANMADI, fit YAPILMADI:** `data/tuik/`'te ısıtma tipine göre ULUSAL hane sayısı veren bir tablo yok (üç dosya da ADNKS hanehalkı büyüklüğü/tipi verisi, ısıtmayla ilgisi yok). Modelin kendi `kombi_hane`/`soba_hane` sayılarını kullanmak dairesel olurdu (parametreyi kendi ürettiği popülasyonla "doğrulamak"), yapılmadı. Parametre fiziksel bandın ([0,9–1,2]) ortasına **1,05** olarak sabitlendi, `config/solid_fuel.py`'a `# VARSAYIM` etiketiyle eklendi.

  **Sonuçlar:** satır sayısı 4.015 ✓; Marmara yıllık hane başı 11.375,8 kWh (kombi çapası) × 1,05 = 11.944,6 kWh/hane/yıl; Marmara toplam yıllık enerji 5,806 TWh; il bazlı tablo **tam HDD sırasını izliyor** (Çanakkale en düşük, Bilecik en yüksek — beklenen, çünkü `q_soba(il)` HDD'nin doğrudan doğrusal ölçeklemesi); Haziran-Ağustos toplamı tam **0,000000** ✓; ek olarak kendiliğinden doğrulanan Aralık-Şubat payı tüm illerde %55-75 bandında (Marmara toplamı %62,7) ✓.

  **İlk koşu bir yönerge tutarsızlığını yakaladı:** `GAS_LEVEL_SOURCE_DTYPE` kategorileri `tuik_national_derived`'ı hiç içermiyordu, oysa yönerge bunu üç yerde (§4.5, §5, §9 JSON örneği) katı yakıtın `level_source`'u olarak adlandırıyordu — NULL kategori hatasıyla yakalandı, kategoriye eklendi.

  **İkinci düzeltme turu — isim değişikliği (kullanıcı talebiyle, kod mantığı değişmedi):** `GAS_LEVEL_SOURCE_DTYPE` → `HEATING_LEVEL_SOURCE_DTYPE` olarak yeniden adlandırıldı — adında "GAS" olması ama artık katı yakıt tarafından da kullanılması (yukarıdaki `tuik_national_derived` ekleme ihtiyacının kendisi bunun semptomuydu) tutarsızlıktı; kardeşi `HEATING_SHAPE_SOURCE_DTYPE` zaten emtia-nötr. Tam metin araması ile `config/gas.py`, `src/build_gas_calibration.py`, `src/build_solid_fuel_calibration.py`, yönerge §5 ve `docs/prompts/` kopyasında değiştirildi, sıfır kalıntı doğrulandı. **Yan not:** yeniden adlandırma sırasında yönergenin ana kopyasını (`C:/workspaces/mds/...`) PowerShell ile düzenlerken bir kodlama bozulması oldu (Türkçe karakterler mojibake'e döndü) — `docs/prompts/` kopyasından (sed ile doğru şekilde değiştirilmişti) geri kopyalanarak düzeltildi, içerik kaybı olmadı.

  **Yeni madde 24 eklendi (ZAYIF — mertebe kontrolü, doğrulama DEĞİL, kalıcı):** `Marmara katı yakıt toplam enerjisi / ulusal katı yakıt konut ısıtması ∈ [%4, %9]`. Ölçülen: 5,806 TWh / ~90 TWh ≈ %6,5 — GEÇTİ. Payda (~90 TWh) TÜİK yüzdelerinden türetilmiş kaba bir tahmin, okunmuş veri değil — yalnız 10× mertebesinde hata yakalar. Katı yakıt katmanının madde 21/23 gibi bir dış çapası YOK ve olmayacak (il bazlı katı yakıt kaynağı mevcut değil) — bu, katmanın zayıflığını `level_source='tuik_national_derived'` etiketiyle birlikte doğrulama listesinde de görünür kılıyor. — onaylayan: yusuf

- [2026-08-25] adim2b-validate-heating-calibration: `src/validate_heating_calibration.py` yazıldı ve koşuldu — Adım 2b'nin `calibration_gas.parquet` + `calibration_solid_fuel.parquet`'ini birlikte denetleyen 26 maddelik doğrulama script'i (Adım 1/2/3 ile aynı desen: her madde `(durum, detay)` döner; dört durum: OK/FARKLI/UYARI/ATLANDI).

  **İki temizlik, listeden önce:** Madde 15 **SİLİNDİ** — `penetrasyon(il)`'in paydası `abone_mesken(il)` hiçbir kaynakta bulunamadı (GAZBİR flip-book, OCR reddedildi), madde uygulanamazdı; yerine madde 20 (abone testi)/21 (dış uzlaşım)/23 (İstanbul çapası) aynı amacı gerçekten yapılabilir biçimde karşılıyor — yönergede silme gerekçesiyle birlikte kayıtlı (sessizce kaybolmadı). Madde 14 **KOŞULLU** yapıldı — `calibration_electricity.parquet` Adım 2 branch'ine ait, bu branch'te bulunamadı, madde ATLANDI etiketiyle geçildi (başarısız sayılmadı).

  **Madde 20/21/23'ün dayandığı sayılar** (önceden yalnız ad-hoc doğrulama betiklerinde satır içiydi) `config/gas.py`'a adlandırılıp kaynaklarıyla donduruldu: `GAZBIR_HANE_BASI_M3_2025`, `KOMBI_DAIRE_YILLIK_M3`, `KARISIM_DUZELTME_ORANI`, `DAIRE_PER_BINA`, `ISTANBUL_KONUT_ABONE_SAYISI_IGDAS`, `ISTANBUL_KOMBI_KONUT_BIRIMI_IGDAS`. Madde 24 için `config/solid_fuel.py`'a `ULUSAL_KATI_YAKIT_ISITMA_TWH_TAHMINI`.

  **Sonuç: 26 madde — 22 GEÇTİ, 2 KALDI, 1 UYARI (madde 21, Kırklareli — bilinen), 1 ATLANDI (madde 14).** Her iki KALDI da aynı kökten geliyor: GAZBİR'in gerçek 2025 serisinde **Mart (182,5) Ocak'tan (154,6) ve Şubat'tan (176,2) yüksek** — meteorolojik olarak alışılmadık ama önceden doğrulanmış gerçek bir veri özelliği. IPF Marmara aylık toplamını bu seriye kilitlediği için: **madde 12** (Bilecik/Edirne/Kırklareli/Tekirdağ'ın Ocak/Ağustos oranı [6,14] bandını aşıyor, 14,03–16,57) ve **madde 13** (76 satır 12 m³/gün tavanını aşıyor, %78'i Mart'ta, en yüksek Kırklareli 4 Mart 16,02 m³/gün, theta_ref=4,66°C — aşırı soğuk bir gün değil). Bantlar DEĞİŞTİRİLMEDİ — kullanıcı önce GAZBİR serisinin takvim ayı mı faturalama dönemi mi olduğunu test etmeyi istedi (Mart'ın yüksek çıkması bir ay kayması sonucu olabilir). Bu turda yalnız validate script'i + adlandırılmış sabitler + madde 15/14 düzeltmeleri commit'lendi, bant değişikliği/kayma düzeltmesi AYRICA ele alınacak. — onaylayan: yusuf
