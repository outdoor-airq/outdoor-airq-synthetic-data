# Kurulum: cp .env.example .env, sonra aşağıdaki değerleri outdoor-airq-core'daki
# .env dosyasıyla aynı yap (aynı timescaledb'ye bağlanıyorlar).
#
# docker-compose.dev.yml bu dosyadan yalnızca DB_USER ve DB_PASSWORD'ü okur.

DB_USER=
DB_PASSWORD=
